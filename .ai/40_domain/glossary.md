# 용어 사전

본 교육 콘텐츠에서 사용하는 핵심 용어를 정의합니다. 정의는 비개발자 독자 기준의 짧은 한 줄을 우선하며, 본문에서 처음 도입되는 페이지를 「출처」로 표기합니다.

| 용어 | 정의 | 비고 |
|------|------|------|
| 생성형 AI | 글·코드·이미지처럼 **새로운 결과물을 만들어내는 AI**. 대표 제품은 Claude·Gemini·ChatGPT. | 출처: `docs/intro.md` |
| 챗봇 | 사람이 묻고 AI가 답하는 **대화형** 사용 방식. 결과를 받은 뒤 처리는 사람이 직접 한다. | 본 교육 1단계 / 출처: `docs/intro.md` |
| 에이전트 | AI가 **직접 파일을 열고 작업을 수행**하는 사용 방식. 사람은 목표만 정해 주고 결과를 검토한다. | 본 교육 3단계 / 출처: `docs/intro.md` |
| claude.ai | Anthropic의 **웹·모바일 대화형 챗봇** 서비스. Projects·Skills 기능으로 자산화·커스터마이징도 가능하다. | 본 교육 1단계 / 출처: `docs/intro.md` |
| Claude Cowork | Claude Desktop에서 동작하는 **비개발자용 범용 에이전트**. 격리 VM·연결 폴더·송신 정책의 3중 보호로 실행된다. | 본 교육 3단계 본격 실습 도구 / 출처: `docs/intro.md` |
| Claude Code | 터미널·IDE에서 동작하는 **코딩 특화 에이전트**. 본 교육에서는 옵션·보조 도구로만 다룬다. | 본 교육 3단계 보조 / 출처: `docs/intro.md` |
| Agent Skills | Claude에 **재사용 가능한 절차·지식을 등록**해 두는 기능. 사전 제작 Skill과 사용자 정의 Skill을 모두 지원한다. 한국어 라벨로는 「에이전트 스킬」을 사용한다. | 출처: `docs/intro.md` 기능 비교표 |
| MCP (Model Context Protocol) | 외부 자료원을 AI 클라이언트에 **표준 방식으로 연결**하는 프로토콜. 커넥터·MCP 서버 연결이 모두 이 프로토콜 위에서 동작한다. | 출처: `docs/basics.md` 외부 컨텍스트 |
| 커넥터 | Claude가 미리 준비해 둔 연결 목록에서 **고르고 로그인·승인만으로** 외부 서비스를 연결하는 방식. 목록 안에서도 Anthropic이 자세히 검토한 항목과 자동 검사만 거친 항목으로 **검토 수준이 갈린다.** 목록의 커넥터도 MCP로 동작하며, 목록은 별개 기술이 아니라 카탈로그다. | 본 교육 2부 / 출처: `docs/connect/connectors.md` |
| MCP 서버 | 기본 커넥터 목록에 없는 도구·자료를 **직접 찾아 등록해** 연결하는 방식. **설정을 어디에 두느냐**로 갈리며, 그 위치가 **접속을 시작하는 주체**를 정한다 — ① **내 계정에 등록**(화면 메뉴 「사용자 정의 커넥터」, 접속은 Anthropic 클라우드 인프라에서 시작하므로 주소가 인터넷에서 열려 있어야 한다) ② **내 컴퓨터에 설정**(Claude Desktop 확장 프로그램, 접속이 내 컴퓨터에서 시작하므로 내 파일과 VPN 안의 사내 자료에도 접근할 수 있다). **설정 위치와 서버가 실행되는 위치는 별개 축**이라, 내 컴퓨터에 설정하고 사내망 주소의 서버에 접속하는 조합이 성립한다(회사 제공 사내 MCP 서버의 전형). 기본 커넥터와 **같은 MCP 위에서 동작**하며, 2부에서 이 용어는 그중 기본 커넥터 목록에 준비돼 있지 않은 쪽을 가리키는 좁은 뜻으로 쓴다. | 본 교육 2부 / 출처: `docs/connect/mcp-servers.md` |
| 결정성 (Deterministic) | **동일한 입력 → 동일한 출력**을 보장하는 동작 성격. 일반 소프트웨어가 여기에 해당한다. | 출처: `docs/basics.md` AI 활용의 세 형태 |
| 비결정성 (Non-deterministic) | **동일한 입력에도 출력이 달라질 수 있는** 동작 성격. 생성형 AI(LLM 호출)는 기본적으로 비결정적이다. | 출처: `docs/basics.md` AI 활용의 세 형태 |
| 1회성 프롬프트 | 챗봇 창에 그때그때 입력하는 **한 번짜리 지시**. 자산화하지 않은 사용 방식. | 출처: `docs/basics.md` AI 활용의 세 형태 |
| 애플리케이션 | 코드로 작성된 일반 **소프트웨어 프로그램**. 결과물 3형태 비교의 한쪽 끝점이다. 이 중 **개인용 HTML·자바스크립트 웹앱**(내 컴퓨터에서 파일 하나를 브라우저로 열면 동작하는 것)까지가 본 교육 범위이고, 여러 사람이 함께 쓰거나 별도 서버·설치·빌드 도구가 필요하거나 웹사이트로 배포하는 앱은 범위 밖이다. | 출처: `docs/basics.md` AI 활용의 세 형태 |
| 하네스 (harness) | AI 모델(생각하는 머리)을 **둘러싼 작업 구조** — 어떤 도구·자료를 쓸지, 어떤 순서로 일할지, 결과를 어디서 확인할지. *사람이 짜 둔 틀*이며 **에이전트 = 모델 + 하네스**로 묶인다. | 출처: `docs/basics.md` 에이전트 = 모델 + 하네스 |
