# Issue #52 감사 리포트 2부(도구·데이터 연결) 슬라이드 신설

> 감사 일시: 2026-08-29  
> 감사 모델: OpenAI, GPT-5  
> 감사 회차: 1차 (이전 리포트: 없음)  
> 감사 대상 브랜치: `issue-0052` (`0515ca3..c26aa33`)  
> 스펙 출처: `./issue-0052-spec.md` (작성 시점 경로는 `.ai/90_issues/active/issue-0052/issue-0052-spec.md`, --clear로 이관)

---

## 종합 의견

슬라이드 구현과 자동 게이트는 충족했다. 32장 전수 대조, Slidev 빌드, MkDocs 엄격 빌드, 접합부·목차·줄표·동기화 점검을 모두 통과했다. 다만 이슈와 무관한 AI 워크스페이스 구조 변경이 브랜치에 포함돼 범위 검증은 미충족이다. 현행 Claude 공식 문서의 커넥터 라벨과 다른 안내도 보정 여부를 결정해야 한다.

## 요약

- 1단계 적합성: 충족(PASS) 12건 / 미충족(FAIL) 1건 / 부분 충족(PARTIAL) 0건 / 판정 불가(N/A) 0건
- 2단계 위험도: 높음(HIGH) 0건 / 중간(MEDIUM) 1건 / 낮음(LOW) 1건 / 정보(INFO) 0건
- 기등재 참조: 0건 (원장 기등재와 일치하는 항목 없음)
- 이전 발견: 해당 없음

| 카테고리 | 건수 |
|----------|------|
| 기능 | 중간(MEDIUM) 1 |
| 아키텍처 | 0 |
| 버그 | 0 |
| 보안 | 0 |
| 코드품질 | 낮음(LOW) 1 |
| 성능 | 0 |
| 테스트 | 0 |

---

## 1단계: 적합성 검증 (Compliance Check)

### 요구사항 대조

| # | 요구사항 | 판정 | 근거 |
|---|----------|------|------|
| 1 | docs 원본 4곳에서 2부 슬라이드를 파생한다 | 충족(PASS) | `slides/slides.md:1483`부터 32장이 있으며, 각 장의 HTML 주석에 원본 매핑이 있다. 원본과 장별 의미를 대조했다. |
| 2 | 1부 접합부를 2부 안내로 전환하고 RAG 내부 구조 범위 밖 선언을 유지한다 | 충족(PASS) | 기존 문구 0건. `slides/slides.md:584`의 외부 컨텍스트 카드가 2부를 안내하고 RAG 범위를 유지한다. |
| 3 | 「오늘의 흐름」에 2부 구간을 반영한다 | 충족(PASS) | `slides/slides.md:37`에 별도 세션인 2부 행이 있다. |
| 4 | #50의 docs 변경 이력 기준으로 1부를 좁게 점검하고 누락만 보정한다 | 충족(PASS) | 점검표에 대상 커밋 12건이 모두 있고 판정은 반영 11건·해당 없음 1건·누락 0건이다. 커밋 목록과 현재 슬라이드를 다시 대조했다. |
| 5 | 변경 범위를 스펙의 포함 범위와 사용자 승인 예외로 제한한다 | 미충족(FAIL) | `c26aa33`이 이슈 스펙에 없는 AI 워크스페이스 구조 8파일을 추가·수정했다. F-1 참조. |

### 완료의 정의(DoD) 대조

| # | DoD 항목 | 판정 | 근거 |
|---|----------|------|------|
| 1 | 2부가 포함된 덱의 Slidev 빌드 통과 | 충족(PASS) | `slides/npm run build` 성공, Slidev 52.14.2에서 894개 모듈 변환 완료. |
| 2 | 1부 접합부 전환 | 충족(PASS) | 금지된 기존 문구 0건, 덱의 2부 안내 존재. |
| 3 | 발행물과 이슈 문서의 U+2014 0건 | 충족(PASS) | 스펙의 `git grep` 명령 결과 0건. |
| 4 | 동기화 점검표에 #50 docs 변경 커밋 전수 등재 | 충족(PASS) | Git 이력 12건과 점검표 행 12건이 일치한다. |
| 5 | 점검표의 의미 판정과 누락 보정 완료 | 충족(PASS) | 모든 행이 허용 판정값을 쓰며 근거가 있다. 누락 판정은 0건이다. |
| 6 | 2부 슬라이드가 docs 원본과 모순되지 않음 | 충족(PASS) | 32장과 원본 4곳의 대응 절을 대조했다. 생략은 발표용 압축 범위이며 반대 의미로 바뀐 항목은 없다. |
| 7 | MCP 서버 슬라이드가 설정 위치·서버 위치 두 구도를 유지 | 충족(PASS) | `slides/slides.md:2208`과 `:2257`에서 두 기준을 별도 장으로 설명하고, 사내망 조합을 유지한다. |
| 8 | 발표 호흡과 오버플로우 점검 완료 | 충족(PASS) | summary Task 6에 사용자 viewport 점검 종료와 미해소 피드백 0건이 기록돼 있다. 이 항목은 스펙이 사용자 직접 판정으로 정했다. |

### 범위 검증

- **스펙 비포함(Out) 침범 여부**: `labs/` 실습, 도구 다원화, MCP 개발, RAG 내부 구조, 컨플루언스 동기화는 건드리지 않았다. 사용자 승인 예외 5파일의 docs 수정도 spec 전제 10과 일치한다.
- **스펙에 없는 추가 구현 여부**: 있음. `c26aa33`은 `.ai/10_rules/`, `.ai/70_ledger/`, `.ai/AI-CONTEXT.md`를 바꿔 이슈 전용 발표 산출물과 독립된 워크스페이스 기능을 도입했다.

### 도메인/계약 정합성

- 계약 문서: 관련 문서 없음.
- 도메인: 용어 사전의 커넥터·MCP 구도와 슬라이드가 일치한다. 현행 공식 라벨과의 차이는 F-2로 별도 기록했다.
- ADR: ADR-0002의 docs → slides 단방향 파생, ADR-0009의 라이트 테마, ADR-0010의 이용 범위·두 기준·보안 SSoT를 준수한다.

---

## 2단계: 비판적 검증 (Critical Review)

### 발견 사항

| # | 위험도 | 영향 | 발생확률 | 카테고리 | 계보 | 설명 | 관련 파일 |
|---|--------|------|----------|------|------|------|-----------|
| F-1 | 낮음(LOW) | 기술 품질 의견 | 통상 사용 | 코드품질 | 신규 | 이슈와 무관한 AI 워크스페이스 구조 변경이 같은 브랜치·PR 범위에 섞였다. | `c26aa33`, `.ai/10_rules/`, `.ai/70_ledger/`, `.ai/AI-CONTEXT.md` |
| F-2 | 중간(MEDIUM) | 기능 저하 | 통상 사용 | 기능 | 신규 | 커넥터 검토 수준을 현행 `Verified`·`Community`·`Custom` 라벨 대신 「자동 검사만」·「표시 없음」으로 안내한다. | `docs/connect/mcp-servers.md:31`, `slides/slides.md:2190`, `.ai/40_domain/glossary.md:15` |

### 상세 분석

#### F-1: 이슈 범위 밖 워크스페이스 구조 변경 혼입

- **위험도**: 낮음(LOW)
- **영향 축**: 기술 품질 의견
- **발생확률 축**: 통상 사용
- **카테고리**: 코드품질
- **관점**: 부작용
- **계보**: 신규
- **권장 조치**: `c26aa33`을 별도 변경 단위로 분리한다. 이 PR에 유지하려면 사용자가 범위 확장을 명시적으로 승인하고 spec·summary에 근거를 남긴다.
- **완료 기준**:
  - `main...issue-0052` 변경 목록에서 스펙과 승인 예외에 없는 워크스페이스 구조 변경이 0건이다.
  - 또는 사용자 승인과 변경 목적·검증 결과가 issue 문서에 명시되고 범위 대조가 통과한다.

<details>
<summary>근거·상세 설명 펼치기</summary>

- **설명**: `c26aa33`은 원장, 산출물 작성 원칙, 컨텍스트 적재 절차를 도입한다. Issue #52의 사용자 대상 슬라이드·접합부·동기화 점검과 독립된 기능이다.
- **영향**: 발표 기능은 깨지지 않지만 리뷰 범위와 롤백 단위가 커진다. 스펙에 없는 변경을 함께 승인하게 된다.

</details>

#### F-2: 커넥터 검토 라벨 안내가 현행 공식 분류와 어긋남

- **위험도**: 중간(MEDIUM)
- **영향 축**: 기능 저하
- **발생확률 축**: 통상 사용
- **카테고리**: 기능
- **관점**: 암묵적 가정
- **계보**: 신규
- **권장 조치**: SSoT인 docs를 먼저 고쳐 `Verified`·`Community`·`Custom` 라벨과 각 검토 수준을 현재 공식 정의대로 설명한다. 용어 사전과 슬라이드를 그 뒤에 맞춘다.
- **완료 기준**:
  - `docs/connect/mcp-servers.md`가 `Verified`는 더 깊은 검토, `Community`는 선별됐지만 심층 검토되지 않음, `Custom`은 Anthropic 미검토로 구분한다.
  - 「자동 검사만 통과」와 「검토 표시가 없으면」처럼 현행 공식 문서에서 확인되지 않는 단정이 관련 docs·용어 사전·슬라이드에서 0건이다.
  - 슬라이드가 화면에서 확인할 실제 라벨과 행동 기준을 연결한다.
  - Slidev 빌드와 MkDocs `--strict` 빌드가 통과한다.

<details>
<summary>근거·상세 설명 펼치기</summary>

- **설명**: 현행 [Claude 커넥터 검증 문서](https://claude.com/docs/connectors/verification)는 디렉터리 항목을 `Verified`와 `Community`로 표시하고, 사용자가 직접 추가한 항목을 `Custom`으로 구분한다. `Community`는 화면에 라벨이 보이며 Anthropic이 선별했지만 심층 검토하지 않은 항목이다.
- **영향**: 현재 슬라이드는 사용자가 실제 화면의 `Community` 라벨을 안전 판단 기준과 연결하지 못하게 한다. 검토 방식도 공식 문서가 보장하지 않는 「자동 검사만」으로 단정한다.

</details>

### 기등재 참조 항목

해당 없음. `.ai/70_ledger/active/`와 `archive/`에 등재 항목이 없다.

### 이전 발견 추적

해당 없음. 초회 감사다.

---

## 검증 기록

<details>
<summary>실행한 검증 펼치기</summary>

- `cd slides && npm run build`: 통과
- `.venv/bin/mkdocs build --strict`: 통과
- 스펙 `[D]` 게이트 4종: 통과
- plan의 2부 배치·32장 제목/순서 전수 게이트: 통과
- plan·summary Task 헤더 집합, 결과값, 수행 모델 게이트: 통과
- `git diff --check main...HEAD`: 통과
- 2부 32장 HTML 발표자 노트 존재 여부: 통과
- 공식 Claude 문서: 커넥터 검증 라벨, 원격·데스크톱 연결, 도구 접근 모드 대조

</details>
