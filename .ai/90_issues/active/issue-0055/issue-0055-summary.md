# Issue #55 실행요약 ― docs 중복 제거·카드 박스 축소·간결화 전체 정리

> 스펙: [issue-0055-spec.md](./issue-0055-spec.md) | 계획: [issue-0055-plan.md](./issue-0055-plan.md)

## 다음 작업

> ▶️ 다음 작업: Task 6 마지막 페이지 `docs/connect/mcp-servers.md` 정리. `connect/index.md`·`connectors.md`는 승인·커밋 완료다. 이 페이지에서 D4 게이트(선택 순서 문장 3파일 합계 2건 이하, 현재 4건)와 K-0001 재검토 조건(`#vs-connector`·`#choosing`)을 함께 판정한다.

## 모델 기록

<!--
이슈 단위 요약(대표값)이다. 세부 SSoT는 아래 `Task별 수행 결과`의 `수행 모델`이며,
한 구분에 여러 모델이 관여했으면 대표 모델을 적고 세부는 Task별 기록을 본다.

형식: "벤더, 모델명". 설계·구현·audit 어느 칸이든 어느 벤더·모델이든 가능하다
(예: Anthropic, Claude Opus 4.8 (claude-opus-4-8) / OpenAI, GPT-5.x / Google, Gemini 3.x).
설계 모델과 구현 모델은 다를 수 있어 칸을 나눈다 ― 같으면 같은 값을 적는다.
"audit 모델 ≠ 구현 모델" 조건을 나중에 명령으로 확인 가능하도록 기록한다. 모델 전환은 사람이 수행하므로 기계로 강제하기 어렵다.
구현에 여러 벤더가 관여했으면 audit 모델의 벤더는 Task별 `수행 모델`의 모든 벤더와도 달라야 한다
― 대표 행 비교만으로는 audit 벤더가 구현 일부에 참여한 경우를 놓친다.
-->

| 구분 | 모델 |
|------|------|
| 설계 모델 | Anthropic, Claude Fable 5 (claude-fable-5) |
| 구현 모델 | Anthropic, Claude Opus 5 (claude-opus-5) |
| audit 모델 | <!-- 구현 모델과 다른 벤더 모델. 형식: 벤더, 모델명. 마지막 교차모델 audit Task에서 사용자가 기록 --> |

---

## Task별 수행 결과

<!--
Task 0 및 일반 실행 Task(Task N 제외) 블록의 지표 4종은 사후 집계용이라 표기를 고정한다
(값이 없어도 필드는 지우지 않는다. Task N 예외의 근거는 아래 Task N 블록 주석).
- 수행 모델: "벤더, 모델명" ― `모델 기록` 표와 같은 형식. 한 Task를 모델 전환·재시도로 둘 이상이 수행했으면
  관여한 모델을 전부 ` / `로 나열한다 ― 하나만 남기면 누락된 벤더가 audit을 맡아도 Task N의 강화 조건을 통과한다.
  `-`는 미착수·스킵 Task 전용이며, `결과`가 완료·부분 완료면 실제 값을 필수로 기록한다
  (`-`로 남기면 Task N의 교차모델 강화 조건이 공집합이 되어 우회가 된다)
- audit 발견: 이 Task와 관련된 audit 발견사항 건수 ― 없으면 `0건` (`-` 금지)
- 보정 반영: `--response` 항목별 승인을 통과해 실제로 보정한 건수 ― 없으면 `0건` (`-` 금지)
- 재시도: 같은 Task에서 재수정·재시도한 횟수 ― 없으면 `0회` (`-` 금지)

한 발견이 여러 Task 산출물에 걸쳐도 가장 직접 수정된 주 Task 하나에만 귀속한다 ―
`audit 발견`·`보정 반영` 모두 그 주 Task에서 1회만 세고 관련된 다른 Task에는 세지 않는다.
Task 축에서도 이슈 단위 합계가 중복으로 부풀지 않게 하기 위함이다(아래 회차 축 중복 제외와 같은 취지).

수치 3종은 회차별 값이 아니라 이슈 전체 누적이다 ― 여러 차수 감사가 같은 발견을 재확인해도
1건으로 세고(중복 제외), 새 회차 값으로 덮어쓰지 않는다. 보정률 사후 집계의 분모·분자가 회차 반복으로 부풀지 않게 하기 위함이다.

수치 3종에 `-`를 쓰지 않는 이유: 아래 스니펫이 `[0-9]+`만 추출하므로 `-`인 Task는
합계에서 조용히 빠지고, 전부 `-`면 합계 자체가 빈 문자열이 된다. `-` 허용은 비수치 필드(`수행 모델`)에만 둔다.

수치 3종의 기본값은 주석이 아니라 리터럴 `0건`/`0회`로 둔다: 주석 placeholder는 렌더링 값이
비어 있는데도 집계기가 주석 안 숫자를 실제 값으로 오인하고, `미기록` 같은 형식 위반도 걸러지지 않는다.

보정률은 별도 필드로 두지 않고 `보정 반영 / audit 발견`으로 사후 집계한다.
아래 스니펫이 동작하도록 `- **<필드>**: <숫자><단위>` 표기를 지킨다 (줄 시작, 굵게, 콜론 뒤 숫자).

  # 이슈 하나의 보정률
  found=$(grep -E '^- \*\*audit 발견\*\*:' issue-0055-summary.md | grep -oE '[0-9]+' | paste -sd+ - | bc)
  fixed=$(grep -E '^- \*\*보정 반영\*\*:' issue-0055-summary.md | grep -oE '[0-9]+' | paste -sd+ - | bc)
  echo "보정률: ${fixed}/${found}"

  # 모델별 집계는 `수행 모델` 행과 함께 뽑는다
  grep -E '^- \*\*(수행 모델|audit 발견|보정 반영|재시도)\*\*:' issue-0055-summary.md
-->

### Task 0 (고정): 구현 시작 게이트 (전제·모호점 확인)

<!--
plan 템플릿의 첫 고정 Task에 대응하는 블록이다. 삭제하지 말 것.
질의로 해소한 전제는 spec `## 전제 (Assumptions)`에 남기고, 여기에는 수행 사실만 적는다.
나열한 항목이 없었으면 `수행 내용 요약`에 `전제 누락 없음` 한 줄을 기록한다.
-->

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Opus 5 (claude-opus-5)
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**: 착수 전 질의 대상 7건(그중 1건이 사용자 판단 불일치 9건 묶음)을 16개 문항으로 풀어 4라운드에 걸쳐 확정했다 (2026-08-30). 확정값은 spec 전제 3·4항에 반영했고 미해소 0건이다. 제안값과 다르게 결정된 항목은 4건이다. ① 카드 개수 상한(52) 기각, 전수 목록 제시 후 사용자 선별로 대체 ② 삭제 기준을 AI 자동 판단이 아니라 페이지별 후보 표 제시 후 사용자 선별로 ③ 홈 배정표는 Task 1로 미뤄 행 단위 승인 ④ `docs/operation-guide.md` 페이지째 삭제.
- **특이 사항**:
    - **범위 변경**: 운영 가이드가 정리 대상에서 삭제 대상으로 바뀌어 정리 대상이 9개다. 이 삭제로 불일치 X1(2단계 도구를 Claude Projects로 적은 곳)과 X8(완료율 80·60 충돌)이 함께 해소되므로 Task 2의 정정 목록에서 뺀다. 파일을 가리키는 참조는 `docs/index.md` 325행과 `mkdocs.yml` 69행 두 곳뿐이다.
    - **Task 1로 넘긴 문서 갱신**: 카드 총수 상한 게이트가 사라져 spec DoD `[D]` 항목이 9종에서 줄고, plan Task 7의 완료율 게이트도 대상이 없어졌다. plan 페이지 Task 공통 절차에 카드 전수 목록과 삭제 후보 표 제시 단계를 넣어야 한다. Task 7 헤더의 `docs/operation-guide.md`도 삭제 작업으로 바꾼다. plan과 summary의 Task 헤더 집합이 일치해야 하는 Task N 게이트가 있으므로 두 파일을 같이 고친다. Task 1에서 함께 반영한다.
    - **미확인 리스크**: X7 확정값(모든 실습을 Claude Desktop의 Claude Cowork에서 진행)이 현재 docs의 제품별 실습 안내와 어디까지 충돌하는지 아직 세지 않았다. Task 2에서 영향 위치를 목록으로 먼저 제시한 뒤 고친다. `labs/step*/` 본문 수정이 필요해지면 spec 비포함 범위와 부딪히므로 그때 사용자에게 다시 묻는다.
    - **Task 2 확인 항목**: 외부 링크는 한국어 페이지 우선이 확정값이라, `claude.com/pricing`과 `docs.claude.com`의 한국어 경로 존재 여부를 실제로 확인한 뒤 URL을 고정한다.

---

### Task 1: 기준 확정 기록과 게이트 착수값 측정

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Opus 5 (claude-opus-5)
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**: Task 0 확정값을 세 문서에 고정하고 홈 배정표 15행을 승인받았다 (2026-08-30). ① spec DoD에서 카드 총수 상한 게이트를 삭제해 `[D]` 항목이 9종에서 8종이 됐고, 허용 유형 목록을 「제안」에서 확정 표기로 바꾸고, strict 빌드 항목에 앵커 검증 포함을 명시했다. ② plan 페이지 Task 공통 절차를 ①~④에서 ①~⑤로 늘려 카드 전수 표·삭제 후보 표 제시를 ②단계로 넣었다. ③ plan Task 7을 「`security-guide.md` 정리와 `docs/operation-guide.md` 삭제」로 재작성하고 완료율 게이트를 파일 부재·잔존 참조 0건 게이트로 교체했다. summary Task 7 헤더도 함께 고쳐 plan과 집합이 일치한다. ④ `mkdocs.yml`에 `validation: anchors: warn`을 추가했다. ⑤ 홈 배정표는 노트 부록 A-1의 15건을 그대로 쓰되 3건을 바꿔 plan 부록 A에 기록했다 (운영 가이드 위치 제외, `labs/README.md` 링크 대상 제외, D11·D14 홈 앵커 특정). 게이트 착수값: G1 strict 빌드 통과 · G2 허용 유형 밖 39건(info 22·abstract 9·success 5·quote 2·failure 1) · G3 같은 라벨 다른 유형 1건 · G4 교차 파일 동일 산문 행 8건(labs 6·connect 2) · G5 U+2014 0건 · G6 외부 링크 각 2종 · G7 낡은 실습 서술 3행 · G8 파일별 `(#55)` 커밋 10개 전부 미보유. 착수 시점 통과가 설계상 예외인 3종(빌드·U+2014·커밋 이력)을 뺀 5종이 전부 실제로 실패하는 상태라 가짜 게이트는 없다.
- **특이 사항**:
    - **앵커 게이트 유효성 확인**: `validation: anchors: warn` 추가 후 고의로 깬 앵커와 죽은 페이지 링크를 각각 넣어 strict 빌드가 중단되는 것을 확인하고 되돌렸다. baseline은 앵커 깨짐 0건이라 회귀 방지 게이트다. 노트 X9(`mcp-servers.md` 140행 「폴더 연결」)는 앵커가 실재하고 가리키는 곳에 내용이 없는 의미 불일치라 이 게이트로는 잡히지 않는다. Task 2가 처리한다.
    - **nav 누락은 strict 빌드가 잡지 못한다**: `mkdocs.yml` nav에서 항목을 빼도 파일이 `docs/`에 남아 있으면 `omitted_files` 기본값이 info라 strict 빌드가 통과한다. 그래서 Task 7의 운영 가이드 삭제 게이트에 파일 존재 검사(`[ -e docs/operation-guide.md ]`)를 따로 넣었다.
    - **Task 0 확정값 정정**: 운영 가이드를 가리키는 참조가 「두 곳뿐」이라던 확정값이 실측과 다르다. 발행물은 두 곳(`mkdocs.yml` 72행, `docs/index.md` 325행)이 맞지만, `.ai/50_adr/active/adr-0001-practice-tool-scope-and-stage-model.md`가 「영향 범위」(6행)와 「후속 조치」(96행)에서 이 파일을 언급한다. spec 전제 3을 실측값으로 고쳤고, ADR을 그대로 둘지 삭제 사실을 병기할지는 Task 7 착수 시 사용자에게 묻도록 plan에 남겼다. ADR은 결정 시점 기록이라 사후 수정이 기본은 아니다.
    - **노트 줄 번호 유효**: 이 브랜치의 두 커밋이 `.ai/`만 건드려 `3e57651` 대비 `docs`·`labs`·`slides` diff가 0이다. 사전 분석 노트의 줄 번호와 홈 배정표의 줄 번호를 그대로 쓸 수 있다. Task 2부터는 본문이 바뀌므로 페이지 착수 시 다시 잰다.
    - **`docs/labs.md` 앵커 부재**: 이 파일에는 명시 앵커가 하나도 없다. D6의 홈으로 삼으려면 「시리즈 구성」에 `{ #series }` 신설이 필요하다. Task 5에서 붙인다.

---

### Task 2: 불일치·용어 정정 (교차 페이지)

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Opus 5 (claude-opus-5)
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**: 파일 8개를 고쳤다 (2026-08-30). **불일치 9건** (X1·X8은 운영 가이드 삭제로 해소): X2 `intro.md` 53행 「세 제품 모두 유료 플랜」을 「Cowork·Code 실습에 유료 플랜」으로 / X3 `security-guide.md` 35행에 「검토 깊이는 영향 범위에 비례하되 밖으로 나가는 셋은 항상 깊게」 단서 신설 / X4 `connect/index.md` 연결 절차를 커넥터·MCP 양쪽 5단계로(「대화에서 켜기」 추가) / X5는 D4 비교표 축약과 한 몸이라 Task 6으로 이월 / X6 모델 문서 URL을 `docs.claude.com/ko/`로, 플랜 URL을 `claude.com/pricing`으로 각 1종화하고 라벨도 「Claude 플랜」으로 / X7 준비물 4곳(`index.md` 71·`labs.md` 59·`labs/README.md` 66·`intro.md` 53)을 「모든 실습에 필요」로 / X9 `intro.md` Cowork 절에 「폴더 연결」 정의 문단을 신설해 `mcp-servers.md` 140행 링크에 실체를 붙임 / X10 기능 비교표 claude.ai 열을 「✗ 로컬」로 고치고 76행 해설을 3제품 구분으로 재작성 / X11 `labs.md`·`labs/README.md` step01 학습 포인트에 「데이터·뷰 분리」 추가. **낡은 2건**: step03 학습 포인트를 `labs/step03-voca-quiz/README.md` 학습 목표 기준 「이미지 입력, 출처 충실성 제약, 외부 사전 링크 자동 연결」로, 「검수 자리」를 「검수 단계」로. **용어·링크**: 한글 음차 「에이전트 스킬」 7곳 전폐(첫 등장 「Agent Skills」·이후 「Skill」), 2단계 대표 도구를 Agent Skills로 통일하며 `index.md` 152행에서 「Claude Projects」 제거, `labs/README.md` 「요금제」→「플랜」, `security-guide.md` 196행 트랙명 표 머리글 축약, `.ai/40_domain/glossary.md` Agent Skills 항목의 한국어 라벨 규정 교체. 게이트 통과: 링크 URL 각 1종 · 낡은 서술 0건 · 「대화에서 켜기」 존재 · strict 빌드 · U+2014 0건.
- **특이 사항**:
    - **X7 범위 사용자 확정 (2026-08-30)**: 「준비물·환경 안내만」으로 결정했다. `labs/` SSoT 실측 결과 step02는 stage 1부터, step03은 stage 2부터 Desktop이 필요해 「stage 3 실습에 필요」는 사실이 틀렸으므로 고쳤다. 반면 `intro.md` 39·50·121·136, `basics.md` 25, `index.md` 153·309~315의 「3단계 실습은 Cowork으로」 서술은 ADR-0001 단계 모델과 맞물려 있어 손대지 않았다.
    - **「요금제」 범위 사용자 확정 (2026-08-30)**: `labs/README.md` 1곳만 고치고 `labs/step*/` 8곳(step01 stage2·stage3, step02 stage1·stage2, step03 stage2, step04 stage2 2곳·stage3)은 spec 비포함 범위라 두었다. 발행물 안에서 용어가 갈리는 상태가 남으므로 #56 이후 별도 정리 대상이다.
    - **「connector 추가」는 오류가 아니었다**: 스크린샷 2장을 직접 확인한 결과 `connectors.md` 49행 「connector 추가」(대화창 `+` → 커넥터 메뉴, 실제로 영문 소문자)와 `mcp-servers.md` 165행 「커스텀 커넥터 추가」(설정 → 커넥터 → 우상단 「추가」 모달 제목)는 **서로 다른 화면의 서로 다른 라벨**이다. UI 라벨은 화면 표기 그대로 두는 규칙에 따라 양쪽 다 유지했다. 두 페이지가 같은 기능을 다른 이름으로 가리켜 독자가 헤맬 수 있으므로 「같은 기능의 다른 진입점」 연결 문장은 Task 6에서 넣는다.
    - **X10 확정 문구를 그대로 쓰지 못했다**: 「로컬 연결은 Desktop 열을 보세요」가 확정값이었으나 기능 비교표에 Desktop 열이 없다(claude.ai·Cowork·Code 3열). Cowork 열이 Desktop 앱을 통한 경로이므로, 76행 해설을 「claude.ai 웹에서는 쓸 수 없고(✗), Cowork·Code에서는 조건부(△)」로 고쳐 같은 뜻을 담았다.
    - **첫 등장 결합을 적용하지 않은 파일 2개**: `docs/labs.md`·`labs/README.md`의 「Skill」은 전부 표 셀과 나열 자리(「마크다운·HTML·엑셀·Skill 지시문」)라 「Agent Skills」로 늘리면 가독성이 나빠진다. 개념을 소개하는 `basics.md`·`index.md`·`intro.md`에만 적용했다.
    - **3단계 매핑 표를 건드린 1곳**: `index.md` 152행에서 「Claude Projects」를 제거했다. 확정값 「2단계 대표 도구는 Agent Skills로 통일」에 따른 것이지만 ADR-0001 서술 경계에 가깝다. Task 3에서 이 페이지를 승인받을 때 함께 확인한다.
    - **커밋 단위가 plan과 다르다**: plan은 「주제별 커밋」이었으나 8개 파일 중 6개가 두 주제 이상에 걸쳐(예: `labs/README.md`는 X7·X11·낡은 서술·요금제) 파일 단위로 쪼갤 수 없어 한 커밋으로 묶었다.

---

### Task 3: `docs/index.md` 정리

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Opus 5 (claude-opus-5)
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**: 326 → 290줄, 카드 26 → 8개로 정리하고 2026-08-30 사용자 검토 완료로 승인받아 `docs: index 정리 (#55)`로 커밋했다. 공통 절차 ③ 초안 반영 후 ④ 점검 루프를 12라운드 돌았다. **카드 처리**: 「사전지식」 2개·「학생 트랙 범위 안내」·「왜 계획부터」·「결정 룰」·Why 3개·1·2단계 example 2개를 본문으로 풀고, 유료 플랜 warning도 본문으로 내렸다. 메타 원칙 요약 `note` 카드 1개를 신설했다. **삭제**: 「어떤 결과물」 표, 「대상별 실행 계획」 절 전체(14줄), 1·2단계 example 대비, 챗봇 vs 에이전트 혼동 방지 문장. **구조**: 인용구를 핵심 선언 전용으로 좁혀 참고·연결 성격 7곳을 본문 문단으로 내렸고, 메타 원칙 다섯 항목 앞에 구분선을 넣었다. **내용**: 결정 룰을 `### AI 활용 3단계와 내 위치` 첫 부분에 편입하되 「결정 룰」 용어는 빼고 본문 비교 목록으로 썼으며, 「실습 접근법」 예시 축을 「프롬프트 구체성」에서 「계획 문서 유무」로 바꿨다. 페이지 게이트 4종 통과.
- **특이 사항**:
    - **Task 0·Task 3 확정값이 점검 루프에서 4건 바뀌었다**: ① 「요구합니다」·「요구하지 않습니다」를 `success`·`failure` 예외 1쌍으로 유지하기로 했다가 카드째 풀어 **예외가 소멸**했다 (spec 범위 In·전제 3·DoD 명령, plan Task 3 게이트 명령에서 예외 grep 2줄을 걷어냈고 허용 유형 착수값을 37건에서 원래 값 39건으로 되돌렸다). ② 「왜 계획부터」 카드 유지(`info`→`tip`)에서 카드째 풀기로. ③ 「대상별 실행 계획」 유지에서 절 전체 삭제로 (사용자: 실습은 그때그때 유동적으로 진행). ④ 결정 룰을 카드로 두는 대신 본문 비교 목록으로.
    - **결정 룰이 용어 없이 편입됐다**: ADR-0005 후속 변경 노트가 「비교 카드로 노출」이라 적고 있으나, 「결정 룰」이라는 말이 비개발자에게 뜻이 통하지 않아 용어를 빼고 본문 목록으로 편입했다. 위치(사다리 진입 직전)와 내용(두 접근 비교)은 유지된다. audit이 문구로 존재를 확인하면 놓치므로 내용으로 대조해야 한다.
    - **D6 배정이 더 깨끗해졌다**: 「어떤 결과물」 표와 「대상별 실행 계획」이 모두 사라져 `index.md`에는 실습 서술이 남지 않고 「함께 읽어보세요」의 한 줄 링크만 남았다. **미결 1건의 대상도 그 줄로 바뀌었다** ― Task 5에서 `labs.md`에 `{ #series }`를 붙일 때 「함께 읽어보세요」의 링크를 `#series`로 승격한다.
    - **번호 표기 결정**: 「AI 활용을 배워야 하는 이유」는 `①②③`을 빼고 ol로 바꿨고, 메타 원칙은 `①`~`⑤`를 유지했다 (본문 상호참조 3곳이 번호로 지칭). 슬라이드가 「이유」 쪽을 `①②③`으로 갖고 있으면 #56에서 함께 맞춘다.
    - **`---` 기호가 두 층위에 쓰인다**: 지금까지 `##` 대분류 사이에만 쓰던 구분선을 메타 원칙 `####` 다섯 항목 앞에도 넣었다 (항목당 11~25줄이라 h4 제목만으로 경계가 약함). 위계가 흐려 보이면 각 원칙을 `###`로 올리는 대안이 있다.
    - **승인 후 사후 정정 1건 (모델 버전)**: 「참고: 컨텍스트 크기는 얼마나 되나요?」 접기의 최신 모델 표기를 「Fable 5」에서 「Fable 5.1」로 고쳤다 (Task 4에서 `intro.md` 버전 예시를 갱신하며 같은 사실이 두 페이지에 있는 것을 발견). Fable 5.1의 컨텍스트 윈도우도 1M이라 숫자 서술은 그대로 두었다. 커밋 `docs: 모델 버전·Academy 설명 정정 (#55)`.
    - **G4 착수값이 8건에서 9건으로 늘었다**: Task 2에서 X7을 정정하며 `docs/labs.md`·`labs/README.md`의 Desktop 준비물 문장을 같은 문구로 고쳐 새 중복이 생겼다. Task 5에서 함께 해소한다.

---

### Task 4: `docs/basics.md`·`docs/intro.md` 정리

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Opus 5 (claude-opus-5)
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**: **`docs/basics.md`**: 165 → 132줄, 카드 9 → 6개로 정리하고 2026-08-30 사용자 검토 완료로 승인받아 `docs: basics 정리 (#55)`로 커밋했다. **카드 풀기 5개**: 「왜 이 분류가 중요한가요?」(quote), 「에이전트 = 모델 + 하네스」(abstract), 「마크다운(Markdown)」(info), 「외부 컨텍스트 ― MCP·RAG라는 용어를 들으셨다면」(info), 「개인용 앱도 다루는 범위가 정해져 있습니다」(note). **카드 유형**: 챗봇·에이전트 대비 쌍을 abstract·tip에서 note로 통일(사용자 선택). info·abstract·quote가 이 파일에서 전폐돼 허용 4종만 남았다. **삭제 3건**: 「AI 활용 3단계와는 별개의 구분」 note(표와 중복), 하네스 조립 요약 인용구, 멀티 에이전트 균형 문단. **제목·라벨**: H2 「챗봇 vs 에이전트」 부제 제거, 카드 라벨 부제 2건 제거, 「(Code는 보조 도구)」 제거. **용어**: 「1회성 프롬프트」를 「1회성 결과물」로 개명하고 비교표 「무엇인가」 셀을 결과물 기준으로 재작성(`docs/basics.md` 4곳 + glossary 1곳). **구조**: 비결정성 warning을 표 아래로 이동, 도입 문장 주어를 「결과물은」으로. **CSS**: 비교표 「결정성」 행 배경 강조. 페이지 게이트 4종 통과. **`docs/intro.md`**: 221 → 209줄, 카드 10 → 5개로 정리하고 2026-09-02 사용자 검사 완료로 승인받아 `docs: intro 정리 (#55)`로 커밋했다. **카드 풀기 2개**: 「본 교육은 Claude로 진행합니다」(info), 「어느 제품이 가장 좋나요?」(tip). 두 문단의 순서를 일반론(어느 제품이 좋은가) → 본 교육의 선택(Claude로 통일)으로 바꿔 산문 흐름을 맞췄다. **카드 삭제 3개**: 「본 교육에서의 위치」(claude.ai `abstract`·Cowork `tip`·Code `note`). 참가 조건에 해당하는 정보만 각 절 본문에 한 문장씩 남겼다 (claude.ai = 익숙한 상태가 참가 전제 / Cowork = Claude Desktop 설치가 참가 조건 / Code = 옵션이라 참가에 불필요). 버린 것은 절 제목·다른 페이지와 겹치던 3건이다 (claude.ai의 Projects·Skills 2단계 무대 서술 D5, Cowork의 「3단계 본격 실습 도구」, Code의 「핵심 실습 무대는 Cowork」 반복). **카드 유형**: 남은 info 2개(「Cowork의 보호 모델」·「Claude Code 사용량 확인 (옵션)」)를 note로 통일해 허용 4종만 남았다(사용자 선택). **내용**: 모델 버전 예시를 「Fable **5.1**, Opus 5, Sonnet 5, Haiku 4.5」로 갱신, 참고 링크에서 「Projects(프로젝트)란 무엇입니까」 줄 삭제, 「Anthropic Learn」(`www.anthropic.com/learn`)을 「Claude Academy」(`academy.claude.com`)로 갱신하며 「*(영문만 제공)*」 표기 제거. 페이지 게이트 4종 통과.
- **특이 사항**:
    - **CSS 변경은 spec 비포함 범위였다**: 「테마·CSS 변경(ADR-0009), `docs/assets/`」가 비포함인데 사용자 요청으로 `docs/assets/css/claude-theme.css`에 비교표 강조 행 규칙 2개를 추가했다(라이트 8%·다크 15% 코랄 틴트). ADR-0009가 정한 팔레트·의미 구분은 건드리지 않고 기존 액센트 `#D97757`을 그대로 쓰며 규칙만 덧붙였다. 행 선택은 `nth-child`가 아니라 첫 칸의 `{ .row-accent }` 클래스 + CSS `:has()`라 행 순서가 바뀌어도 따라간다. audit이 범위 이탈로 보지 않도록 여기 남긴다.
    - **3단계 매핑이 사라졌다 (ADR-0008 관련)**: 삭제한 note 카드에는 표와 겹치지 않는 내용이 하나 있었다. 「AI 활용 3단계」와 이 절의 세 형태가 **다른 기준의 분류**라는 것과 그 매핑(① 챗봇 사용 ≈ 1회성 결과물 / ②·③ ≈ Skill / 그 너머 = 애플리케이션)이다. ADR-0008이 「3단계 모델과 차원이 다르다」로 정한 내용이고 #42가 이름 혼동을 풀려고 넣은 장치다. 도입 문단에 한 문장으로 남기는 안을 제시했으나 사용자가 검토 완료를 알려 현재 상태(완전 제거)로 확정했다. **ADR-0008의 결정 자체는 유효하며 docs 표면에서만 빠진 상태다.** spec DoD의 ADR 정합 audit 항목이 이 자리를 지적하면 이 결정이 근거다.
    - **「1회성 프롬프트」 개명의 범위 밖 잔존**: 이 라벨은 ADR-0008이 정의하고(`:30`·`:40`) ADR-0005(`:69`)·ADR-0011(`:13`)·`.ai/50_adr/index.md`(`:23`)가 인용한다. 분류 자체는 그대로고 이름만 바뀐 것이라 결정 변경은 아니지만 ADR은 결정 시점 기록이라 손대지 않았다. Task 1이 Task 7로 미뤄 둔 ADR-0001 운영 가이드 언급 건과 **함께 Task 7에서 사용자에게 묻는다**(그대로 둘지, 후속 변경 노트로 개명 사실만 병기할지). `slides/slides.md` 2곳(`:546` 제목, `:554` 표 머리글)은 #56 대상이다.
    - **glossary 출처 표기가 낡았다 (미해결)**: `.ai/40_domain/glossary.md`의 결정성·비결정성·1회성 결과물·애플리케이션 4개 항목이 출처를 「`docs/basics.md` AI 활용의 세 형태」로 적고 있으나 그 절 이름은 #42에서 「결과물의 성격」으로 바뀌었다. 이번에 항목명만 고치고 출처 표기는 사용자 판단 대기 상태로 두었다.
    - **공통 절차 ②를 쓰지 않았다**: plan은 카드 전수 표와 삭제 후보 표를 먼저 제시하고 사용자가 항목별로 정하는 절차인데, 이 페이지는 사용자가 대상을 직접 지목하는 방식으로 진행됐다. 항목별 사용자 선별이라는 Task 0 확정값의 취지는 그대로 지켜졌다(AI 자동 판단 없음).
    - **`docs/intro.md`에 넘길 확인 1건**: 삭제한 3단계 매핑을 `intro.md`가 대신 언급하고 있는지. 「1회성 결과물」 개명과 `intro.md` 107행의 「1회성 작업」은 원인·결과 관계라 톤이 맞는다고 2026-08-30 사용자가 확인했다.
    - **`intro.md`에 3단계 매핑은 없었다 (넘긴 확인 1건 종결)**: `basics.md`에서 삭제한 「결과물의 성격 3형태 ↔ AI 활용 3단계」 매핑(ADR-0008)을 `intro.md`가 대신 언급하는지 확인한 결과, 이 페이지는 제품별 위치(「1단계의 시작점」·「3단계 본격 무대」·「3단계 보조 도구」)만 절 제목에서 말하고 결과물 성격과의 매핑은 갖고 있지 않다. **매핑은 docs 표면에서 빠진 상태 그대로다** (근거는 위 basics 항목).
    - **「Claude Academy」 한국어 지원은 사용자 확인값이다**: 사용자가 「한글로 지원한다」고 알려 「*(영문만 제공)*」 표기를 제거했다. 페이지를 조회했을 때 메타 정보에서 한국어 지원 여부는 확인되지 않았고, 사이트 명칭이 「Claude Academy」로 바뀐 것만 확인했다(라벨·URL도 그에 맞춰 갱신). audit이 표기 근거를 물으면 이 항목이 근거다. 링크 설명도 사이트 실제 구성(제품별 학습 가이드 5종 + 강좌 과정)에 맞춰 「프롬프트 작성·활용 가이드 모음」에서 「제품별 학습 가이드와 강좌 모음」으로 바꿨다.
    - **`docs/intro.md`도 공통 절차 ②를 쓰지 않았다**: `basics.md`와 같은 이유로 카드 전수 표·삭제 후보 표 대신 사용자가 대상을 직접 지목하는 방식으로 진행됐다. AI 자동 판단은 없었고, 유형 선택(info → note 2건)은 선택지를 제시해 사용자가 골랐다.
    - **docs에서 Projects 언급이 사라졌다 (#56 후속 3곳)**: `docs/intro.md` 참고 링크의 「Projects(프로젝트)란 무엇입니까」 줄을 사용자 지시로 삭제했다. Task 2가 `index.md` 152행에서 「Claude Projects」를 뺀 것과 합쳐, Task 7의 운영 가이드 삭제(`operation-guide.md` 44행이 마지막 언급)까지 마치면 발행 대상 docs의 Projects 언급은 0건이 된다. `slides/slides.md`는 3곳(`:1007` 3단계 매핑 표의 2단계 도구, `:3163` 참고 링크, `:3192` 링크 그룹 압축 주석)이 남아 있어 **#56에서 함께 걷어내야 한다**. `labs/step04-wrong-answer-note/stage2-wrong-answer-note-skill/README.md`의 「Project로도 운영하기」 절(`:237`~`:247`)은 Skill 대신 Project로 운영하는 대안 안내이며 ADR-0006상 실습 본문 SSoT라 이 이슈·#56 모두 범위 밖이다.

---

### Task 5: `docs/labs.md`·`labs/README.md` 정리

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Opus 5 (claude-opus-5)
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**: `docs/labs.md` 59 → 48줄, 카드 2 → 0개. `labs/README.md` 67 → 69줄(카드 없음). 2026-09-02 사용자 승인. **카드 풀기 2개**: 「① 프롬프트로만 진행합니다」·「② 결과물은 반드시 읽고 이해합니다」(둘 다 `tip`). 사용자 지시로 하위 목록은 그대로 두고 굵은 라벨로 시작하는 별도 문단으로 전환했다. 이 페이지 카드는 0개가 됐다. **삭제 2건**: 「왜 사이트 안에 본문이 없나요?」 H2 절(최상단 blockquote와 중복. 이유 문구 「로컬에서 따라하기 좋도록」만 blockquote로 흡수), 사전 준비의 「핵심 준비물」 불릿 2개(D1 홈이 `index.md#preparation`). **문구**: 「둘은 짝입니다.」 삭제. **교차 파일 동일 산문**: 착수 7건(착수 시점 6건 + Task 2의 X7 정정으로 1건 증가) → 0건. **D6**: `docs/labs.md` 「시리즈 구성」에 `{ #series }` 앵커를 신설하고 `docs/index.md` 「함께 읽어보세요」 링크를 `labs.md#series`로 승격해 Task 3이 넘긴 미결 1건을 종결했다. **R1**: `labs/README.md` 사전 준비를 「Claude Pro 이상 유료 플랜」 한 줄에서 트랙 구분 2줄로 늘려 임직원 개인 플랜 금지를 넣었다. `docs/labs.md`의 금지 문구가 D1 링크화로 사라져 labs 계열에 남지 않게 되는 것을 막기 위함이다. **zip 배포물**: `.gitattributes`를 신설해 소스 zip에 `labs/`만 남겼다. 페이지 게이트 4종 + 교차 파일 동일 산문 + 낡은 실습 서술 통과.
- **특이 사항**:
    - **spec 범위를 확장했다 (2026-09-02)**: 「docs에 노출된 main.zip은 나중에 교체해야 하지?」라는 사용자 질의에서 시작했다. 처음에 원장 이관을 제안했으나 사용자가 이번 이슈에 있어야 한다고 지적해 바로잡았다. 근거는 **의존 관계**다. `docs/labs.md`의 방어 문구(「`labs/` 디렉토리만 열어 사용」)를 지우는 간결화가 zip 내용물 정리에 의존하므로, zip을 미루면 docs 쪽 간결화도 못 한다. spec `## 범위` 포함에 한 줄, 전제 3에 확정값을 기록했다.
    - **`.gitattributes`는 repo 인프라 변경이다**: spec 비포함이 `scripts/`·`overrides/`·`docs/assets/` 같은 인프라를 빼 놓았으므로 결이 같다. 예외로 포함한 근거는 위 의존 관계이며 전제 3에 있다. 제외 대상은 `labs/`를 뺀 최상위 전부이고, 애매한 `README.md`·`requirements.txt`·`.gitignore`와 `.gitattributes` 자신도 사용자 확정으로 함께 뺐다. audit이 범위 이탈로 보지 않도록 여기 남긴다.
    - **동일 산문 해소 방향이 spec 설계 주의와 반대다**: spec DoD의 설계 주의는 「`labs/README.md` 원문을 남기고 `docs/labs.md` 쪽을 링크로 바꿔 해소」였다. 그러나 사용자가 카드를 풀되 **하위 목록은 그대로 두라**고 지시해 docs 쪽 문구가 보존됐고, 대신 `labs/README.md` 4행의 어순·표현을 바꿔 해소했다(뜻은 그대로). D2 배정표의 「README 원문 유지(자기완결)」와도 어긋나지만, 자기완결성 자체는 유지됐다(내용을 링크로 빼지 않고 표현만 바꿈). spec 84행과 부록 A D2는 착수 시점 서술이라 고치지 않았다.
    - **zip 실물 검증 완료 (2026-09-02)**: 로컬 `git archive --worktree-attributes`로 최상위 `labs/` 1개(파일 42개)를 확인한 뒤, 사용자 확정대로 **머지 전에 브랜치를 푸시해** `archive/refs/heads/issue-0055.zip`을 실제로 받아 확인했다. 최상위는 `labs/` 하나뿐이고 크기는 56KB다. GitHub 소스 zip이 `export-ignore`를 따른다는 것이 실물로 확인됐다. 명령은 plan Task 5 완료 기준 접기에 있다.
    - **공통 절차 ②를 썼다**: Task 3·4와 달리 카드 전수 표(2개)와 삭제 후보 표(S1~S3 + README R1)를 먼저 제시하고 사용자가 항목별로 정했다.
    - **`main.zip` 링크는 그대로 두었다**: 시점 고정(태그 zip)·Release 첨부는 채택하지 않았다. `export-ignore`가 노출 문제만 정확히 해결하고 docs 링크 변경이 0이기 때문이다. 교육 회차별 시점 고정이 필요해지면 `archive/refs/tags/<태그>.zip`으로 바꾸는 것이 다음 수단이다 (이 이슈 범위 밖).

---

### Task 6: `docs/connect/` 3페이지 정리

- **결과**: 부분 완료 (3페이지 중 2페이지 승인·커밋, `mcp-servers.md` 미착수)
- **수행 모델**: Anthropic, Claude Opus 5 (claude-opus-5)
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**: **`docs/connect/index.md` (완료)**: 94 → 89줄, 카드 9 → 4개. 2026-09-03 승인·커밋 `db0134a docs: connect 개요 정리 (#55)`. 카드 풀기 5개(`abstract` 「연결은 1부 3단계와 나누는 기준이 다릅니다」·`info` 「용어는 1부에서 이미 정의했습니다」·`info` 「준비물」·`tip` 「제품마다 연결 지원 범위가 다릅니다」·`note` 「이용까지 다루고, 만드는 일은 다루지 않습니다」), 유형 통일 1개(`success` → `example` 「연결한 뒤」)로 허용 4종 밖 0건. 「제품마다 연결 지원 범위가 다릅니다」는 본문으로 내리면서 명령줄 도구 `note` 앞으로 옮겨 표에 바로 이어지게 했다. 「고르는 순서는 간단합니다」 인용구를 일반 문단으로, 「1부에 이어서 진행됩니다」 절의 이탤릭 강조 3곳 제거. **사다리 은유를 단계 서술로 정정**했고(`connect/index.md` 본문 + `intro.md` 「함께 읽어보세요」 링크 설명) docs 내 잔존 0건이다. **`docs/connect/connectors.md` (완료)**: 193 → 176줄, 카드 15 → 8개. 2026-09-05 승인. **카드 풀기 7개**: 13행 `info` 「1부에서 이미 나온 이야기입니다」(라벨은 첫 문장이 같은 뜻을 담아 흡수) · 61행 `tip` 「쓸 만큼만 켜 두세요」 · `warning` 「AI가 쓴 것도 내 이름으로 팀에 공개됩니다」 · `info` 「학생·일반인: 읽기만이라면 연결하지 않아도 됩니다」 · `warning` 「임직원: 업무 도구가 따로 있다면 연결하지 않는 쪽을 권합니다」 · `info` 「목록에 서비스가 안 보인다면 권한 문제일 수 있습니다」 · `warning` 「연결해도 되는지는 회사 정책이 정합니다」. 라벨을 굵은 리드 문장으로 승격하고 하위 목록은 그대로 두는 방식이다. **GitHub 절 재구성**: 기존 `example` 「이런 부탁이 됩니다」(읽고 정리하는 예시 2개)를 「GitHub ― 애플리케이션 개발하기」로 교체해 개발 활동 예시 3개(이슈 파악 → 구현 → 코드 리뷰)를 담고, 첫 문단 바로 뒤로 올려 Atlassian 절 예시 카드와 자리를 맞췄다. 카드 아래 문단 6개는 목록 6항목으로 요약하고, `note` 「저장소를 프로젝트에 통째로 붙이는 별도 경로도 있습니다」를 절 맨 아래로 옮겼다. **중복 제거 4건**: 「커넥터는 로그인과 권한 확인이 필요한 일에 쓰는 수단」 절 내 2회 → 1회(D11), GHE 서술 2곳 → 1곳(D12 홈이 이 파일), 「연결하기로 정했다면 다룰 수 있는 범위는 이렇습니다」 예고 문장 삭제, 「목록에서 저장소가 보이지 않는다면 대개 이 승인이 빠진 것」 삭제(같은 「목록에 안 보이면 권한 문제」 구조가 `#enterprise` 절과 `connect/index.md` 72행에 이미 있어 세 번째 반복이었다). **문구 정정**: 이탤릭 `*외부 서비스 연결*` 제거, 「그 칸의 실물입니다」 → 「그 실제 예입니다」, 「"고르고·연결만 하는 것" **칸에**」 → 「**「고르고·연결만 하는 것」**에」, 「학생·일반인:」·「임직원:」 트랙 접두 제거, 공개 저장소에 `(Public)` 표기 1회 추가. 인용부호 2건: 명칭 지칭 자리를 낫표로, 「나는 권한이 넓은 편인가」를 작은따옴표로. 페이지 게이트 4종 전부 통과(**허용 유형 밖 3 → 0건** · 같은 라벨 다른 유형 0건 · U+2014 0건 · strict 빌드 통과).
- **특이 사항**:
    - **실행 순서가 plan과 다르다**: plan Task 6은 `mcp-servers.md` → `connectors.md` → `index.md` 순인데 실제로는 `index.md`를 먼저 했고(2026-09-03), 이어 nav 순서대로 `connectors.md`를 진행 중이다. `connectors.md`가 D4의 홈(`mcp-servers.md#vs-connector`)을 링크로 가리켜야 하지만 그 앵커는 착수 전부터 실재해 순서를 바꿔도 링크가 깨지지 않는다. 남은 `mcp-servers.md`는 그대로다.
    - **`connect/index.md` 회차의 수행 모델은 다른 세션이라 미확인**: 커밋 `db0134a`는 별도 세션(`session_0168pFXQGo2CYusfyWXgBdxo`)에서 나왔고 그 세션의 모델을 이 세션에서 확인할 방법이 없다. 위 `수행 모델`은 `connectors.md` 회차의 값이며, 이 이슈 내내 구현 모델이 같았다는 전제로 한 줄만 적었다. **다르면 사용자가 ` / `로 병기해야 한다** ― Task N의 교차모델 강화 조건이 Task별 `수행 모델`의 모든 벤더와 audit 벤더를 대조하므로, 누락된 벤더가 audit을 맡으면 조건이 우회된다.
    - **「칸」은 사실과 달랐다**: `connectors.md` 13행이 1부의 「고르고·연결만 하는 것」을 「칸」으로 지칭했으나, `basics.md#model-harness`의 그 자리는 표가 아니라 grid 안의 admonition 카드 2개 중 하나이고 「외부 서비스 연결」은 그 안의 불릿이다. 사용자가 지적한 것은 뒤 문장(「그 칸의 실물」)뿐이었지만 앞 문장의 「칸에」도 같은 오류라 함께 걷어냈다.
    - **「쓸 만큼만 켜 두세요」는 라벨을 살려야 했다**: 51행이 `(→ 바로 아래 「쓸 만큼만 켜 두세요」)`로 이 카드 라벨을 지목하고 있어, 라벨을 지우면 참조가 갈 곳을 잃는다. 굵은 리드 문장으로 승격했고(`docs/labs.md` 진행 원칙 2카드와 같은 방식), 대신 마지막 문단의 「어느 모드든 지금 하는 일에 필요한 것만 켜는 편이 결과도 깔끔합니다」를 뺐다. 라벨 승격으로 같은 말이 한 문단 안에 두 번 남는 것을 막기 위함이며, **되살릴지는 사용자 판단 대기**다.
    - **인용부호 규정을 확인하고 Task 8로 이관했다**: 「한글 맞춤법」 부록 *문장 부호* 기준으로 큰따옴표는 직접 대화·직접 인용, 작은따옴표는 마음속으로 한 말·특별히 드러내는 부분, 홑낫표는 소제목·명칭이다. 이 페이지의 프롬프트 예시 15개는 직접 인용이라 큰따옴표가 규정에 맞아 낫표로 바꾸지 않는다. `docs/basics.md`의 소제목 `### 하네스는 결국 "내가 세팅하는 것들"`은 그 제목을 인용하는 곳이 함께 따라가야 해 페이지 단위로 처리할 수 없으므로, **plan Task 8 작업 내용 3번과 완료 기준 `[ND]` 1종을 신설**해 옮겼다(착수값 45건, 판정 목록 추출 명령 포함). 기존 작업 내용 3·4번은 4·5번으로 밀렸다.
    - **K-0001 재검토 조건은 아직 해당 없음**: `mcp-servers.md`의 `#vs-connector`·`#choosing`을 아직 손대지 않았다. 그 페이지 착수 시 판정한다.
    - **D4 게이트는 미통과 상태**: 「목록에 있으면 커넥터」 취지 문장이 3파일 합계 4건(`connect/index.md` 1 · `connectors.md` 1 · `mcp-servers.md` 2)으로 착수값 그대로다. 기준은 2건 이하이며, `connectors.md` 191행(S6)과 `mcp-servers.md` 축약에서 해소한다.

---

### Task 7: `docs/security-guide.md` 정리와 `docs/operation-guide.md` 삭제

- **결과**: <!-- 완료 / 부분 완료 / 스킵 -->
- **수행 모델**: -
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**: <!-- 페이지별 처리 목록, 카드 수 전후, 승인 일자, 커밋 -->
- **특이 사항**:

---

### Task 8: 전체 게이트 재실행·통독·동반 갱신

- **결과**: <!-- 완료 / 부분 완료 / 스킵 -->
- **수행 모델**: -
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**: <!-- DoD 8종 결과, 통독 라운드, nav·glossary 갱신 여부, 착수 대비 diff 규모(줄 수·카드 수) -->
- **특이 사항**:

---

### Task N (고정): 교차모델 issue-audit 검증 (사용자 수동 수행)

<!--
plan 템플릿의 마지막 고정 Task에 대응하는 블록이다. 삭제하지 말 것.
사용자가 직접 수행하며, 구현 AI는 이 블록을 대신 채우지 않는다.
지표 4종을 두지 않는다 ― 수행 모델은 위 `모델 기록` 표의 `audit 모델` 행이 SSoT이고,
audit 발견·보정 반영은 이 Task가 만들어낸 값이라 각 대상 Task 블록에 집계된다.
-->

- **결과**: <!-- 완료 / 부분 완료 / 스킵 -->
- **수행 내용 요약**: <!-- audit 리포트 위치, 발견사항 건수, `--response` 검토 결과 -->
- **특이 사항**:
