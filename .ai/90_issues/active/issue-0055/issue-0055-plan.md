# Issue #55 실행계획 ― docs 중복 제거·카드 박스 축소·간결화 전체 정리

> 스펙: [issue-0055-spec.md](./issue-0055-spec.md)

---

## 설계 종료 게이트 (고정)

<!--
이 블록은 모든 이슈 계획에 고정한다. 삭제하지 말 것.
plan 작성을 끝내기 직전에 수행하는 자기점검이며, 실행 Task가 아니라 "plan 작성 절차"라 Tasks 앞에 둔다.
문서를 쓴 주체와 읽는 주체가 다를 수 있다는 것만 전제한다 ― 다른 모델이든, 세션 교체 후의 같은 모델이든 같다.
-->

> **점검 질문**: 이 spec/plan만 보고, 작성에 참여하지 않은 쪽이 구현에 필요한 내용을 스스로 알 수 있는가?

- [x] 점검 완료
- **점검 대상** (작성 중 머릿속에만 있었던 것):
  - 코드베이스 관례 ― 이 repo에서만 통하는 패턴·명명·배치 규칙
  - 버전·환경 제약 ― 특정 버전·플랫폼·도구에 묶인 조건
  - 검토 후 버린 대안과 그 이유
  - 사용자와의 합의로만 정해진 값
- **발견 시 조치**: spec `## 전제 (Assumptions)` 섹션에 적는다. 발견이 없으면 그 섹션에 "없음" 한 줄을 남긴다.
- **이번 점검 결과**: 10건을 발견해 spec `## 전제 (Assumptions)`에 반영했다 (사전 분석 노트와 줄 번호 기준, 사용자 확정값 4종, Task 0 확정 대상의 제안값, 줄표 규칙, 콘텐츠 서술 규칙 10항, 기술 전제, 버린 대안 4건, 작업 순서 근거, 커밋 단위, `labs/README.md` 자기완결).

---

## Tasks

> AI가 순서대로 실행할 작업 단위를 정의합니다.
> **작성 원칙**: 구현자에게 이 문서 외 컨텍스트가 없다고 가정하고, Task별 완료 기준을 결정적으로 씁니다.
> 각 작업은 독립적으로 검증 가능해야 하며, 완료 기준은 **항목별 리스트로 쪼개 레벨 태그 + 검증 수단**을 적습니다.
> 명령으로 환원이 어려운 항목만 판정 주체(다른 AI / 사람)를 적고 강등 사유를 남깁니다.
>
> **완료 기준 형식** ― 본문(접기 금지)에는 레벨 태그 + 무엇이 보장되는지 한 문장(+ 강등 사유)만 두고,
> 검증 명령·테스트케이스·기대 출력·명령 설계 주의점은 접기로 내립니다. 접기 제목에 기대 출력을 함께 적습니다
> (기본형: "검증 명령 ― 출력 0건이면 통과"). 접을 상세가 없는 항목은 접기 없이 본문 한 줄로 둡니다.
> 다른 게이트의 선행 조건이 되는 항목은 의존 관계와 **선행 조건이 무너졌을 때의 결과**를 본문 문장에 남기고, 메커니즘만 접기에 둡니다.
> 첫 고정 Task(Task 0 구현 시작 게이트)와 마지막 고정 Task(교차모델 issue-audit)는 삭제하지 말고 그대로 둡니다.
>
> **검증 레벨** ― 낮을수록 좋다(자동 검증에 가까움). 기본은 L1, 한 레벨 내릴 때마다 강등 사유를 함께 적는다.
>
> - `[D]`  L1 결정적   ― 명령이 합/불을 판정, 사람 판단 없음
> - `[QD]` L2 준결정적 ― 다른 AI·기준 체크리스트가 채점
> - `[ND]` L3 비결정적 ― 사람이 직접 읽고 판단
>
> **문서를 대상으로 검증할 때는 문구가 아니라 행 구조를 셉니다**. 같은 문구가 주석·안내문에도 등장하므로
> `grep -c '<문구>'`는 실제 구조가 없어도 통과합니다. 헤더는 `^## `, Task는 `^### Task `,
> 필드는 `^- \*\*…\*\*:`로 앵커를 고정하고, 명령은 작업 디렉토리에 의존하지 않게 repo 루트 기준 경로로 적습니다.
> **검증 명령 보정은 반례 격추가 아니라 불변식 전수 명세로 합니다**. 지적된 변형 하나만 막으면 이웃 변형이
> 다음 감사에서 재발하므로, 기대 구조를 열거하고 "그 외 0건"까지 판정에 넣습니다.
>
> **페이지 Task(3~7) 공통 절차**: ① 사전 분석 노트 부록 A-1·A-2·C에서 이 페이지 관련 항목을 뽑아 처리 목록을 먼저 제시한다 → ② **카드 전수 표**(줄번호·현재 유형·라벨·담긴 내용 한 줄·유지/풀기 제안과 근거)와 **삭제 후보 표**(줄범위·내용 요약·제안 사유)를 제시하고 사용자가 항목별로 정한다 → ③ 확정된 선택대로 초안을 반영한다(중복은 홈 또는 링크로, 유지 카드는 4종으로 유형 통일, 풀기 카드는 본문으로, 선별된 삭제 후보를 지우고 남은 내용을 간결화, 불일치·용어 정정) → ④ 사용자가 점검하고 피드백을 반영하기를 반복한다 → ⑤ 사용자가 「승인」을 명시하면 페이지 게이트를 재실행하고 `docs: <페이지> 정리 (#55)`로 커밋한다. 승인 전에는 다음 페이지에 손대지 않는다.
> ②를 건너뛰고 AI가 카드·삭제를 스스로 판단하지 않는다 (Task 0 확정). 카드 수는 게이트가 아니므로 정리 전후 값을 summary 해당 Task에 기록만 한다.
> **페이지 게이트** = spec DoD [D] 중 파일 한정 실행이 가능한 3종(허용 유형 외 0건, 같은 라벨 다른 유형 0건, U+2014 0건)을 그 파일에만 돌린 것 + strict 빌드. 명령은 Task 3 접기에 한 번만 적고 이후 Task는 참조한다.

### Task 0 (고정): 구현 시작 게이트 (전제·모호점 확인)

<!--
이 블록은 모든 이슈 계획의 첫 Task로 고정한다. 삭제하지 말 것.
설계 종료 게이트가 "쓰는 쪽"의 자기점검이라면, 이 게이트는 "읽는 쪽"이 착수 전에 거는 확인이다.
spec을 쓴 주체와 구현하는 주체가 같아도 수행한다 ― 세션이 바뀌면 전제는 똑같이 유실된다.
-->

- [x] 완료
- **목표**: 구현에 필요하지만 문서만으로는 알 수 없는 전제·모호점을 코드 작성 전에 걷어낸다.
- **작업 내용**:
  1. spec/plan을 읽고, 구현에 필요하지만 문서만으로는 알 수 없는 전제·모호점을 나열한다.
  2. 항목이 있으면 **코드를 쓰기 전에 사용자에게 질의**하고, 답변을 spec `## 전제 (Assumptions)` 섹션에 반영한 뒤 구현을 시작한다.
  3. 항목이 없으면 summary Task 0의 `수행 내용 요약`에 `전제 누락 없음` 한 줄을 기록하고 진행한다.
- **이 이슈의 착수 전 질의 대상** (spec 작성 시점에 이미 식별된 미결 항목. 전제 3 참조):
  1. 홈 배정표: 노트 부록 A-1의 홈 후보 15건을 그대로 쓸지, 바꿀 주제가 있는지
  2. 카드 기준·허용 유형 4종·상한 52개 채택 여부
  3. 삭제 기준 문구 채택 여부
  4. 사용자 판단 불일치 9건의 방향 (유료 플랜 필요 이유 / 완료율 80·60 / step01 데이터·뷰 / 검토 강도 / 2단계 도구 / Desktop 시점 / 스킬 명칭 / URL / claude.ai 로컬 표기)
  5. 결정 룰 편입 위치 (`index.md` `## What` 또는 `basics.md` 결과물의 성격 앞)
  6. `mkdocs.yml`에 `validation: anchors: warn`을 추가해 strict 빌드가 앵커 깨짐(노트 A-2 X9 유형)을 잡게 할지
  7. 페이지 처리 순서(Task 3~7)와 페이지별 커밋 단위 동의 여부
- **완료 기준**:
  - [ND] 나열한 항목이 전부 spec `## 전제 (Assumptions)`에 반영되어 미해소 0건이거나, summary Task 0에 `전제 누락 없음`이 기록된다  (검증: 사람 리뷰)  ← 강등 사유: 전제·모호점을 빠짐없이 나열했는지는 의미 판단이라 명령으로 환원 불가

---

### Task 1: 기준 확정 기록과 게이트 착수값 측정

- [x] 완료
- **목표**: Task 0 확정값을 문서에 고정하고, DoD [D] 게이트가 착수 시점에 실제로 실패하는지 실측해 가짜 게이트를 걷어낸다.
- **작업 내용**:
  1. Task 0 답변을 spec `## 전제 (Assumptions)` 3항에 확정값으로 반영하고, 제안값과 다르면 DoD 검증 명령의 임계값(허용 유형 목록·상한)을 함께 고친다.
  2. 홈 배정표를 이 문서 말미 `## 부록 A ― 홈 배정표`에 표로 기록한다 (주제 / 홈 파일·절 / 링크로 바꿀 위치).
  3. DoD [D] 명령 8종을 전부 실행해 착수 시점 값을 summary Task 1에 기록한다. 통과해야 할 게이트가 착수 시점에 이미 통과하면(빌드·U+2014·커밋 이력 제외) 게이트 설계를 의심하고 보정한다.
  4. Task 0에서 `validation: anchors: warn` 추가로 정했으면 `mkdocs.yml`에 반영하고 strict 빌드 baseline을 다시 잰다.
- **완료 기준**:
  - [D] 홈 배정표가 이 문서에 표로 존재하고 행이 15개 이상이다
    <details>
    <summary>검증 명령 ― 출력 0건이면 통과</summary>

    ```bash
    P=.ai/90_issues/active/issue-0055/issue-0055-plan.md
    n=$(awk '/^## 부록 A/{a=1} a && /^\| D[0-9]+ /{c++} END{print c+0}' "$P"); [ "$n" -ge 15 ] || echo "위반: 홈 배정표 행 ${n}개 < 15"
    ```

    - 설계 주의: 행 앵커는 `| D<번호> `로 고정한다. 노트 부록 A-1의 번호 체계를 그대로 쓴다.
    </details>
  - [D] summary Task 1의 `수행 내용 요약`에 DoD 게이트 8종의 착수값이 기록된다 (`게이트 착수값:` 행 존재)
    <details>
    <summary>검증 명령 ― 출력 0건이면 통과</summary>

    ```bash
    S=.ai/90_issues/active/issue-0055/issue-0055-summary.md
    awk '/^### Task 1:/{a=1} /^### Task 2:/{a=0} a && /게이트 착수값:/{f=1} END{if(!f) print "위반: Task 1 게이트 착수값 미기록"}' "$S"
    ```

    </details>
  - [ND] 사용자가 홈 배정표와 확정 임계값을 승인한다  (검증: 사람 리뷰)  ← 강등 사유: 배정의 타당성은 사람 판단

---

### Task 2: 불일치·용어 정정 (교차 페이지)

- [x] 완료
- **목표**: 페이지 정리에 앞서 「무엇이 정답인가」를 docs 전체에 먼저 고정한다. 어느 사본을 남길지는 정답이 정해진 뒤에야 정할 수 있다.
- **작업 내용**:
  1. 노트 부록 A-2의 11건을 Task 0 확정 방향으로 정정한다. 판단이 필요 없는 건(X4 절차 5단계, X5 표 행 구성, X9 「폴더 연결」 정의, X7 Desktop 시점)도 여기서 함께 처리한다.
  2. docs가 낡은 2건을 정정한다. `docs/labs.md`·`labs/README.md`의 step03 학습 포인트를 `labs/step03-voca-quiz/README.md` 학습 목표 기준으로, `labs/README.md`의 「검수 자리」를 「검수 단계」로.
  3. 용어·링크를 통일한다. 재사용 자산 명칭, 플랜 비교 URL, 모델 문서 URL, 트랙명 축약, `connectors.md`의 「connector 추가」(실제 화면 확인 후).
  4. 용어 정의가 바뀌면 `.ai/40_domain/glossary.md`를 함께 고친다.
  5. 주제별로 커밋한다 (`docs: <주제> 불일치 정정 (#55)`).
- **완료 기준**:
  - [D] spec DoD의 링크 URL 1종 게이트와 낡은 실습 서술 0건 게이트가 통과한다  (검증: 해당 명령 재실행)
  - [D] 커넥터 연결 절차가 개요와 본문에서 같은 단계 수다. `connect/index.md`에 「대화에서 켜기」 서술이 존재한다
    <details>
    <summary>검증 명령 ― 출력 0건이면 통과</summary>

    ```bash
    grep -q '대화에서 켜' docs/connect/index.md || echo '위반: connect/index.md 절차에 5단계(대화에서 켜기) 없음'
    ```

    </details>
  - [QD] A-2 11건이 각각 확정 방향으로 정정되고 다른 쪽 사본이 함께 바뀌어 잔존 불일치 0건  (검증: 교차모델 audit이 spec 전제 대 본문 대조 채점)  ← 강등 사유: 의미 대조라 명령으로 환원 불가

---

### Task 3: `docs/index.md` 정리

- [x] 완료
- **목표**: 홈이 가장 많은 페이지(D1·D2·D5·D6·D8, 카드 26개)를 먼저 확정해 다른 페이지가 링크할 대상을 만든다.
- **작업 내용**:
  1. 공통 절차 ①~⑤를 수행한다. 이 페이지의 처리 대상은 최소 다음이다: 메타 원칙 ①~⑤ 카드를 본문 절로, 「왜 계획부터」(D8)를 메타 원칙 ①로 흡수, 「어떤 결과물」 표와 「대상별 실행 계획」(D6) 중 하나로 통합, 「요구합니다/요구하지 않습니다」 대비 쌍 유지 여부 판단, Why 3카드 유형 정리, 결정 룰 편입(Task 0에서 이 페이지로 정했을 때).
  2. ADR-0005의 메시지 3곳(도입부 한 줄·핵심 인용구·결정 룰)이 남아 있는지 확인한다.
- **완료 기준**:
  - [ND] 사용자가 이 페이지의 승인을 명시하고 summary에 승인 일자가 기록된다  (검증: 사람 리뷰)  ← 강등 사유: 승인은 사용자 행위
  - [D] 페이지 게이트 통과 (파일 한정 3종 + strict 빌드)
    <details>
    <summary>검증 명령 ― 출력 0건이면 통과 (F를 대상 파일로 바꿔 Task 4~7에서 재사용)</summary>

    ```bash
    F=docs/index.md
    grep -nE '^\s*(!!!|\?\?\?\+?) +[a-z]+' "$F" | sed "s|^|$F:|" \
      | grep -vE '^[^:]+:[0-9]+:\s*(!!!|\?\?\?\+?) +(warning|tip|note|example)\b'
    grep -oE '^\s*!!! [a-z]+ "[^"]+"' "$F" | sed -E 's/^ *!!! ([a-z]+) "(.*)"/\2|\1/' | sort -u | cut -d'|' -f1 | uniq -d
    BAD=$(printf '\xe2\x80\x94'); grep -n "$BAD" "$F"
    .venv/bin/mkdocs build --strict -d "${TMPDIR:-/tmp}/issue-55-site" >/dev/null 2>&1 || echo '위반: MkDocs strict 빌드 실패'
    ```

    - 설계 주의: 허용 유형 목록은 Task 1에서 확정한 값과 같아야 한다. spec DoD 명령을 고쳤으면 여기도 같이 고친다.
    - 파일 한정 실행이라 `grep -n` 출력에 파일명이 없으므로 `sed`로 `$F:` 접두를 붙여 spec 쪽 명령과 입력 형태를 맞춘다.
    </details>
  - [D] 이 파일에 `(#55)` 커밋이 존재한다  (검증: `git log --format=%s main..HEAD -- docs/index.md | grep -q '(#55)'`)

---

### Task 4: `docs/basics.md`·`docs/intro.md` 정리

- [x] 완료
- **목표**: 1부 나머지 두 페이지를 정리한다. 용어 홈(챗봇 vs 에이전트, 모델+하네스, 결과물의 성격)과 제품 소개의 중복·카드를 정리한다.
- **작업 내용**:
  1. 공통 절차 ①~⑤를 페이지별로 수행한다(승인·커밋도 페이지별). 최소 처리 대상: `basics.md`의 「3단계와는 별개의 구분」 note(D5, `index.md` 172행과 통합), 챗봇/에이전트·모델/하네스 정의 카드를 본문으로, 비결정성 warning의 가능성형 표현 확인. `intro.md`의 「본 교육에서의 위치」 3카드 통합, 원격/로컬 해설(D7)을 `mcp-servers.md` 링크로, 제품별 3단계 반복(D5) 정리, 사용량 확인 절의 tip 정리.
  2. ADR-0008의 비교표(`basics.md` 첫 H2)는 유지한다.
- **완료 기준**:
  - [ND] 사용자가 두 페이지 각각의 승인을 명시하고 summary에 기록된다  (검증: 사람 리뷰)  ← 강등 사유: 승인은 사용자 행위
  - [D] 두 파일 각각 페이지 게이트 통과  (검증: Task 3 접기 명령을 `F=docs/basics.md`, `F=docs/intro.md`로 재실행)
  - [D] 두 파일 각각 `(#55)` 커밋 존재  (검증: `git log --format=%s main..HEAD -- <파일> | grep -q '(#55)'`)

---

### Task 5: `docs/labs.md`·`labs/README.md` 정리

- [x] 완료
- **목표**: 실습 인덱스와 zip 동봉 README의 복붙 중복(6행)을 README 자기완결을 지키며 푼다.
- **작업 내용**:
  1. 공통 절차 ①~⑤를 수행한다. `docs/labs.md`의 진행 원칙 ①·②를 `labs/README.md` 링크와 한 줄 요약으로 바꾸고, 사전 준비(D1)를 `index.md#preparation` 링크로, 시리즈 구성 표를 `labs/README.md` 표와 같은 내용으로 맞춘다.
  2. `labs/README.md`는 원문을 유지하되 Task 2에서 정정한 step03·「검수 단계」를 반영한 상태인지 확인한다.
  3. (2026-09-02 사용자 추가 확정) `.gitattributes`를 신설해 GitHub 소스 zip에 `labs/`만 남기고, `docs/labs.md` 「자료 받기」의 방어 문구(「`labs/` 디렉토리만 열어 사용」)를 지운다. 근거와 대상 목록은 spec 전제 3의 「zip 배포물 정리」에 있다.
- **완료 기준**:
  - [ND] 사용자가 두 파일의 승인을 명시하고 summary에 기록된다  (검증: 사람 리뷰)  ← 강등 사유: 승인은 사용자 행위
  - [D] spec DoD의 교차 파일 동일 산문 행 게이트에서 `docs/labs.md`↔`labs/README.md` 쌍이 0건  (검증: 해당 명령 재실행 후 출력에 `labs` 포함 행 0건)
  - [D] 두 파일 각각 페이지 게이트 통과 (README는 admonition이 없으므로 U+2014·빌드만)  (검증: Task 3 접기 명령 재실행)
  - [D] 두 파일 각각 `(#55)` 커밋 존재  (검증: `git log --format=%s main..HEAD -- <파일> | grep -q '(#55)'`)
  - [D] GitHub 소스 zip 최상위에 `labs/`만 남는다. 커밋 전 로컬 검증과 푸시 후 실물 검증을 모두 거친다
    <details>
    <summary>검증 명령 ― 출력이 `labs` 한 줄이면 통과</summary>

    ```bash
    # 커밋 전 (worktree의 .gitattributes를 반영)
    git archive --worktree-attributes --format=tar HEAD | tar -t | cut -d/ -f1 | sort -u

    # 푸시 후 실물 (머지 전 브랜치 zip)
    curl -sL https://github.com/scroogy-dev/ai-onboarding/archive/refs/heads/issue-0055.zip -o "${TMPDIR:-/tmp}/issue-0055.zip"
    unzip -Z1 "${TMPDIR:-/tmp}/issue-0055.zip" | cut -d/ -f2 | sort -u
    ```

    - 설계 주의: GitHub의 소스 zip은 최상위에 `<repo>-<ref>/` 한 겹을 더 씌우므로 실물 검증은 `cut -d/ -f2`로 한 단계 안쪽을 본다. 로컬 `git archive`에는 그 겹이 없어 `-f1`이다.
    </details>

---

### Task 6: `docs/connect/` 3페이지 정리

- [ ] 완료
- **목표**: 2부 세 페이지(카드 46개)의 3중 서술(D3·D4·D10·D11·D14)과 카드 본문화를 정리한다.
- **작업 내용**:
  1. 공통 절차 ①~⑤를 페이지별로 수행한다. 순서는 `mcp-servers.md`(홈: D4·D7) → `connectors.md`(홈: D3 화면·D9·D10·D12·D14) → `index.md`(대부분 링크로 축약).
  2. 최소 처리 대상: `mcp-servers.md`의 원격/로컬 2카드와 「내 컴퓨터에 설정해야 되는 때」 3카드를 본문 절로, 토큰 범위 서술(D3)을 security-guide 링크 + 「기간」만 남김. `connectors.md`의 메뉴 경로 2카드를 절차 목록으로, 승인 화면 절(D3)을 화면 홈으로 확정. `connect/index.md`의 비교표(D4)를 2행으로, 준비물(D1)·관리자 정책(D10)을 링크로.
  3. `mcp-servers.md` `#vs-connector`·`#choosing`을 손대면 K-0001 재검토 조건에 해당하므로 summary 특이 사항에 재검토 결과를 남긴다.
- **완료 기준**:
  - [ND] 사용자가 세 페이지 각각의 승인을 명시하고 summary에 기록된다  (검증: 사람 리뷰)  ← 강등 사유: 승인은 사용자 행위
  - [D] 세 파일 각각 페이지 게이트 통과  (검증: Task 3 접기 명령 재실행)
  - [D] 「목록에 있으면 커넥터, 없을 때만 MCP」 취지의 선택 순서 문장이 connect 3파일 합계 2건 이하다 (D4 축약 확인. 착수 시점 4건)
    <details>
    <summary>검증 명령 ― 출력 0건이면 통과</summary>

    ```bash
    n=$(grep -cE '목록에 있(으면|었다면)|먼저 그쪽 목록을' docs/connect/index.md docs/connect/connectors.md docs/connect/mcp-servers.md | awk -F: '{s+=$2} END{print s+0}'); [ "$n" -le 2 ] || echo "위반: 선택 순서 문장 ${n}건 > 2"
    ```

    - 설계 주의: 착수 시점 4건(`connect/index.md` 61, `connectors.md` 191, `mcp-servers.md` 29·309)이라 실제로 실패하는 상태에서 시작한다. 정리 중 문구를 바꿔 쓰면 이 grep을 빠져나가므로, Task 6 착수 시 실제 문장으로 패턴을 다시 맞춘다.
    </details>
  - [D] 세 파일 각각 `(#55)` 커밋 존재  (검증: `git log --format=%s main..HEAD -- <파일> | grep -q '(#55)'`)

---

### Task 7: `docs/security-guide.md` 정리와 `docs/operation-guide.md` 삭제

- [ ] 완료
- **목표**: `security-guide.md`의 파일 내부 3중 서술(D13)과 connect 쪽 재서술(D3·D9·D11·D12)을 정리하고, 강사용 `operation-guide.md`를 참조까지 함께 걷어낸다.
- **작업 내용**:
  1. `security-guide.md`에 공통 절차 ①~⑤를 수행한다. 절대 입력 금지 정보(D13)를 표 1곳으로, 권한 최소화(D3)는 원칙만 남기고 화면은 `connectors.md` 링크, 연결 점검(D9)·GHE 문단(D12)을 링크로, 검토 강도(X3)를 전제 4의 확정 방향(영향 범위 비례를 원칙으로 두고 외부 공유·개인정보·사실 확인은 항상 깊게 본다는 예외 단서)으로 고친다.
  2. `docs/operation-guide.md`를 삭제한다. 파일 삭제 + `mkdocs.yml` nav 항목 제거 + `docs/index.md` 「함께 읽어보세요」의 링크 줄 제거를 한 커밋(`docs: 교육 운영 가이드 삭제 (#55)`)에 담는다. 삭제는 공통 절차 ②(카드 표·삭제 후보 표)의 대상이 아니다. 남길 내용을 고르는 작업이 아니라 페이지 전체를 걷어내는 작업이라 항목별 선별이 성립하지 않는다.
  3. ADR-0001이 이 파일을 「영향 범위」(6행)와 「후속 조치」(96행) 두 줄에서 언급한다. ADR은 결정 시점의 기록이라 사후 수정이 기본은 아니므로, 그대로 둘지 삭제 사실을 병기할지 착수 시 사용자에게 묻고 결과를 summary 특이 사항에 남긴다.
- **완료 기준**:
  - [ND] 사용자가 `security-guide.md` 승인과 `operation-guide.md` 삭제를 각각 명시하고 summary에 기록된다  (검증: 사람 리뷰)  ← 강등 사유: 승인은 사용자 행위
  - [D] `security-guide.md`가 페이지 게이트 통과  (검증: Task 3 접기 명령을 `F=docs/security-guide.md`로 재실행)
  - [D] `docs/operation-guide.md`가 실제로 사라지고 발행물(`docs/`·`mkdocs.yml`)에 잔존 참조가 0건이다. strict 빌드는 죽은 링크·nav 항목은 잡지만 파일이 남아 있는 것은 잡지 못하므로, 파일 존재 여부는 이 게이트에만 있다
    <details>
    <summary>검증 명령 ― 출력 0건이면 통과</summary>

    ```bash
    [ -e docs/operation-guide.md ] && echo '위반: docs/operation-guide.md 잔존'
    grep -rn 'operation-guide' docs mkdocs.yml
    ```

    - 설계 주의: 파일 존재와 참조를 함께 본다. 링크만 지우고 파일을 남기면 strict 빌드가 통과하므로(nav에 없는 파일은 `omitted_files` 기본값이 info라 조용히 지나간다) 첫 줄이 필요하다. `.ai/` 하위는 이력 문서라 대상에서 뺀다.
    </details>
  - [D] 두 파일 각각 `(#55)` 커밋 존재 (`operation-guide.md`는 삭제 커밋이 이력이 된다)  (검증: `git log --format=%s main..HEAD -- <파일> | grep -q '(#55)'`)

---

### Task 8: 전체 게이트 재실행·통독·동반 갱신

- [ ] 완료
- **목표**: 페이지별 승인이 끝난 상태에서 전체 DoD를 한 번에 재실행하고, 페이지를 넘나드는 흐름(링크 방향·용어)을 사용자가 통독해 확정한다.
- **작업 내용**:
  1. spec DoD [D] 8종을 전부 재실행해 결과를 summary Task 8에 기록한다. 실패가 있으면 해당 페이지로 돌아가 보정하고 그 페이지의 승인을 다시 받는다.
  2. `mkdocs.yml` nav 라벨과 `.ai/40_domain/glossary.md`가 최종 본문과 맞는지 확인한다.
  3. 사용자가 nav 순서대로 전체를 통독하고 페이지 간 흐름 피드백을 준다. 피드백이 소진될 때까지 반영한다.
  4. 착수 커밋(`6bad27c`) 대비 최종 diff의 규모(줄 수·카드 수 전후)를 summary에 기록한다. #56이 대조 범위를 잡는 데 쓴다.
- **완료 기준**:
  - [D] spec DoD `[D]` 항목 8종 전부 통과  (검증: 해당 명령 재실행)
  - [ND] 사용자가 통독 종료를 선언하고 미해소 피드백이 0건이다  (검증: 사람 리뷰)  ← 강등 사유: 흐름·가독성은 사람 판단

---

### Task N (고정): 교차모델 issue-audit 검증 (사용자 수동 수행)

<!--
이 블록은 모든 이슈 계획의 마지막 Task로 고정한다. 삭제하지 말 것.
audit은 L2 [QD] 보완 검증 ― L1 [D] 결정적 게이트의 대체가 아니라 보완이다.
이 Task는 사용자가 직접 수행하며, 구현 AI는 자동으로 닫지 않는다.
-->

- [ ] 완료
- **목표**: 스펙 위반·누락·소스코드와의 모순을 구현 모델과 다른 시각으로 잡는다.
- **실행 주체**: **사용자가 직접** 수행한다. 구현 AI는 이 Task를 **자동으로 닫지 않으며**, `issue-audit`를 자동 실행하지도 않는다.
- **작업 내용**:
  1. spec `완료의 정의`의 `[D]` 항목 검증 명령을 전부 재실행해 통과를 확인한다.
  2. 구현을 수행한 모델과 **다른 벤더 모델**(최소 동급 이상 역량)로 사용자가 직접 `issue-audit`를 실행한다. 방향은 칭찬이 아니라 허점 탐색("스펙 위반·누락·소스코드와의 모순을 찾아라").
  3. 사용자가 audit 결과(지적 사항·감사 모델)를 summary에 반영·기록한다. 발견사항 보정은 issue-work `--response`로 검토한다. **피드백 먼저, 항목별 승인 후에만** 앞 Task를 보정하며, 리포트를 받자마자 자동 보정하지 않는다.
- **완료 기준**:
  - [D] spec `완료의 정의`의 `[D]` 항목 검증 명령 전부 통과  (검증: 해당 명령 재실행)
  <!-- 아래 5개 항목은 순서가 아니라 의존 관계다: 선행 조건(Task 집합 일치 → 결과 확정 → 수행 모델 채움)이 서지 않으면 교차 벤더 비교가 공집합이 되어 통과처럼 보인다. -->
  - [D] summary의 Task 헤더 집합이 plan과 일치한다. 블록 전체 누락·Task N 누락·빈 summary·두 경로 오기를 차단하며, 아래 두 게이트의 선행 조건이라 블록 자체가 없으면 그 두 게이트는 검사 대상이 사라져 그대로 통과한다
    <details>
    <summary>검증 명령 ― repo 루트에서 실행, 출력 0건이면 통과</summary>

    ```bash
    P=.ai/90_issues/active/issue-0055/issue-0055-plan.md
    S=.ai/90_issues/active/issue-0055/issue-0055-summary.md
    { grep -qE '^### Task ' "$P" && grep -qE '^### Task ' "$S" \
      && diff <(grep -E '^### Task ' "$P") <(grep -E '^### Task ' "$S") \
      || echo '위반: 입력 접근 실패 또는 Task 집합 불일치'; }
    ```

    - 설계 주의: 프로세스 치환 안의 `grep` 실패는 `diff` 종료 상태로 전파되지 않아 두 경로가 모두 잘못되면 빈 입력끼리 비교해 통과한다. 선행 `grep -q`로 두 파일의 Task 헤더 실재를 먼저 확인하고 실패를 stdout 위반 행으로 환원한다.
    - 의존 근거: 아래 두 게이트는 `^### Task ` 블록 안의 행만 훑는 awk라 블록 유무 자체를 판정할 수 없다. 그 판정은 이 게이트에만 있다.
    </details>
  - [D] 이 검증 전에 Task 0 및 모든 일반 실행 Task의 summary `결과`가 완료·부분 완료·스킵 중 하나로 확정된다. Task N 제외 블록마다 유효 `결과` 행 정확히 1개, 무효·중복 0건이며, 아래 `수행 모델` 게이트의 선행 조건이라 비워 두면 그 게이트의 검사 대상이 공집합이 되어 통과처럼 보인다
    <details>
    <summary>검증 명령 ― repo 루트에서 실행, 출력 0이면 통과</summary>

    ```bash
    S=.ai/90_issues/active/issue-0055/issue-0055-summary.md
    awk '
      /^### Task / { if (o && !n && v != 1) b++; o = 1; v = 0; n = ($0 ~ /^### Task N/) }
      o && /^- \*\*결과\*\*:/ {
        if ($0 ~ /^- \*\*결과\*\*: (완료|부분 완료|스킵)[[:space:]]*$/) v++
        else if (!n) b++
      }
      END { if (o && !n && v != 1) b++; print b+0 }
    ' "$S"
    ```

    - 설계 주의: 총개수 비교는 한 블록의 미확정을 다른 블록의 중복 행으로 상쇄해도 통과하므로 블록 단위로 센다.
    - 의존 근거: 아래 게이트의 "`수행 모델` 값 필수" 조건은 `결과`가 완료·부분 완료인 블록에서만 발동하도록 걸려 있다.
    </details>
  - [D] 완료·부분 완료 Task의 `수행 모델`이 비어 있지 않고 `-`도 아닌 행 정확히 1개(`-`는 미착수·스킵 전용)이며, 아래 교차 벤더 조건의 선행 조건이라 전부 `-`로 남기면 그 조건이 공집합이 되어 우회가 된다
    <details>
    <summary>검증 명령 ― repo 루트에서 실행, 출력 0이면 통과</summary>

    ```bash
    S=.ai/90_issues/active/issue-0055/issue-0055-summary.md
    awk '
      /^### Task / { if (o && !n && d && (t != 1 || m != 1)) b++; o = 1; d = 0; t = 0; m = 0; n = ($0 ~ /^### Task N/) }
      o && /^- \*\*결과\*\*: (완료|부분 완료)[[:space:]]*$/ { d = 1 }
      o && /^- \*\*수행 모델\*\*:/ { t++; if ($0 ~ /^- \*\*수행 모델\*\*: [^-[:space:]]/) m++ }
      END { if (o && !n && d && (t != 1 || m != 1)) b++; print b+0 }
    ' "$S"
    ```

    - 설계 주의: 리터럴 `-`만 거부하면 빈 값·행 누락·중복이 통과하므로 "없음"의 세 형태와 중복을 전부 실패로 센다.
    - 의존 근거: 아래 교차 벤더 조건은 비어 있지 않은 `수행 모델` 값에서만 벤더를 뽑아 대조한다.
    </details>
  - [QD] summary `모델 기록` 표의 `구현 모델`·`audit 모델` 두 행이 "벤더, 모델명" 형식으로 채워지고 서로 다른 벤더  (검증: 교차모델 audit이 두 행 대조 채점)  ← 강등 사유: 벤더 토큰 추출이 자유 문자열 의미 대조라 명령으로 환원하면 표기 변형에 취약하다
  - [QD] 구현에 여러 벤더가 관여했으면 audit 모델의 벤더가 Task 0 및 일반 실행 Task의 비어 있지 않은 모든 `수행 모델` 값에 나열된 벤더 전부와도 상이  (검증: 교차모델 audit이 `수행 모델` 행 전수 추출 후 audit 벤더와 대조 채점)  ← 강등 사유: 위와 같음. 표의 대표값 비교만으로는 audit 벤더의 구현 참여를 놓치고, 한 Task를 여러 모델이 수행했으면 그 값에 나열된 벤더를 모두 비교 대상에 넣는다
  - [ND] audit이 칭찬이 아니라 허점 탐색 방향으로 수행됨  (검증: 사용자가 audit 리포트 내용으로 판단)  ← 강등 사유: 감사 방향성은 리포트 서술의 의미 판단이라 명령으로 환원 불가

---

## 부록 A ― 홈 배정표

> 2026-08-30 사용자 승인 확정. 행 앵커는 `| D<번호> |`. 노트 부록 A-1의 번호를 그대로 쓴다.
> 「홈」은 그 주제의 본문 원본을 두는 **절**이고, 「링크로 바꿀 위치」는 본문 대신 한 줄 링크만 남길 곳이다.
> 사이트 nav·페이지 순서와는 무관하다 (nav 구조 변경은 spec 비포함).

| 번호 | 주제 | 홈 (파일 · 절) | 링크로 바꿀 위치 |
|------|------|----------------|------------------|
| D1 | 유료 플랜 필수 · 개인 플랜 금지 · 준비물 | `index.md#preparation` | `labs.md` 57~59 / `connect/index.md` 74 / `connectors.md` 185 / `security-guide.md` 86·165 |
| D2 | AI 결과물 검토·이해 의무 / 틀릴 수 있다 | `index.md#meta-principles` ③ | `docs/labs.md` 27~30. `labs/README.md` 원문 유지(자기완결). `connectors.md` 117~120·`mcp-servers.md` 275~278·`basics.md` 72~81·`security-guide.md` 32~38은 각도가 달라 유지 |
| D3 | 권한 최소화 · 「내 권한 그대로」 · 승인 범위 | 이원화: 원칙은 `security-guide.md#external-connection`, 화면은 `connectors.md#approval-screen` | `connectors.md` 81~88의 원칙 서술 / `mcp-servers.md` 213~222는 「기간」만 남기고 링크 / `connect/index.md` 78~79 |
| D4 | 커넥터 vs MCP 서버 비교표 · 선택 순서 | `mcp-servers.md#vs-connector` | `connect/index.md` 53~61 표를 2행으로 축약 / `connectors.md` 5·191 / `mcp-servers.md` 309~310 |
| D5 | 3단계 모델 · 「3단계 ≠ 결과물 세 형태」 | `index.md#stage-model` | `basics.md` 87~97 통합 / `intro.md` 110·121·136 제품별 반복을 1회로 / `connect/index.md` 13~16 / `labs.md` 36 |
| D6 | 실습 결과물 예시 · step 매핑 | `docs/labs.md` 「시리즈 구성」 (34행, 앵커 `{ #series }` 신설) | `index.md` 174~179 표와 303~318 중 하나를 삭제하고 링크로. `labs/README.md` 표는 자기완결 예외라 유지 |
| D7 | 원격 vs 로컬 (설정을 어디에 두느냐) | `mcp-servers.md#remote-vs-local` | `intro.md` 78~82 삭제, 83 링크만 유지 |
| D8 | 계획 → 실행 분리 | `index.md#meta-principles` ① | `index.md` 199~206을 ①로 흡수 / `index.md` 276 / `basics.md` 57은 이미 링크형 |
| D9 | 연결 점검·해제 3개 불릿 | `connectors.md#manage-disconnect` | `security-guide.md` 145~151 / `mcp-servers.md` 273은 이미 링크형 |
| D10 | 회사 관리자 정책 · 「목록에 없으면 담당 부서 문의」 | `connectors.md#enterprise` | `connect/index.md` 76 / `mcp-servers.md` 177~180·287 / `security-guide.md` 166 |
| D11 | 연결이 필요한 일은 로그인·권한 확인이 필요한 일 | `connect/index.md#from-part1` (13~20행) | `connectors.md` 133·142를 1회로 / `security-guide.md` 175 |
| D12 | GitHub Enterprise 라이선스·로그인 문단 | `connectors.md#github` | `security-guide.md` 176 |
| D13 | 절대 입력하면 안 되는 정보 | `security-guide.md#never-input` 표 | 파일 내부 3중: 90~103 학생 트랙 예시 · 197~200 정리 표 · 126~132를 표 1곳으로. `mcp-servers.md` 224~225는 이미 링크형 |
| D14 | 커넥터/MCP 토글을 켜야 대화에서 쓰인다 | `connectors.md#how-to-connect` 5단계 | 같은 파일 51·60·87을 1회로 / `mcp-servers.md` 169·286 |
| D15 | 챗봇 vs 에이전트 정의 | `basics.md#chatbot-vs-agent` | `intro.md` 116 / `index.md` 136·151~153 |

**노트 부록 A-1에서 바꾼 곳 3건** (2026-08-30 승인)

1. D1·D2·D5의 등장 위치에서 `operation-guide.md`를 뺐다. Task 7에서 페이지째 삭제되므로 링크로 바꿀 대상이 아니다.
2. D2·D6의 링크 대상에서 `labs/README.md`를 뺐다. zip 동봉 자료라 자기완결을 지킨다 (spec 전제 11). 그 쌍의 중복은 `docs/labs.md` 쪽만 링크로 바꿔 푼다.
3. D11·D14의 홈을 실제 앵커로 특정했다. 노트는 각각 「`connect/index.md`」·「`connectors.md` 연결하는 순서 5단계」로만 적혀 있었다.

**앵커 신설 1건**: D6의 홈 `docs/labs.md` 「시리즈 구성」에는 명시 앵커가 없다 (이 파일에 명시 앵커가 하나도 없음). Task 5에서 `{ #series }`를 붙인다. 절 제목·순서는 그대로라 nav에 영향이 없다.
