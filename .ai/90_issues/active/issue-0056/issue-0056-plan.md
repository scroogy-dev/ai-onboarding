# Issue #56 실행계획 ― slides docs 전수 동기화 (드리프트 정정·누락 절 편입, #55 후속)

> 스펙: [issue-0056-spec.md](./issue-0056-spec.md)

---

## 설계 종료 게이트 (고정)

<!--
이 블록은 모든 이슈 계획에 고정한다. 삭제하지 말 것.
plan 작성을 끝내기 직전에 수행하는 자기점검이며, 실행 Task가 아니라 "plan 작성 절차"라 Tasks 앞에 둔다.
문서를 쓴 주체와 읽는 주체가 다를 수 있다는 것만 전제한다 ― 다른 모델이든, 세션 교체 후의 같은 모델이든 같다.
-->

> **점검 질문**: 이 spec/plan만 보고, 작성에 참여하지 않은 쪽이 구현에 필요한 내용을 스스로 알 수 있는가?

- [x] 점검 완료
- **점검 대상** (작성 중 머릿속에만 있었던 것):
  - 코드베이스 관례 ― 이 repo에서만 통하는 패턴·명명·배치 규칙
  - 버전·환경 제약 ― 특정 버전·플랫폼·도구에 묶인 조건
  - 검토 후 버린 대안과 그 이유
  - 사용자와의 합의로만 정해진 값
- **발견 시 조치**: spec `## 전제 (Assumptions)` 섹션에 적는다. 발견이 없으면 그 섹션에 "없음" 한 줄을 남긴다.
- **점검 결과 (2026-09-08)**: 발견 14건을 spec 전제 1~14에 적었다. 노트 줄 번호의 유효 범위(slides 유효·docs 무효), #55 이월 항목 6건, B-3 9건의 현재 위치, 용어 대응표, 구조 슬라이드 14장, 덱 관례, 줄표·서술 규칙, 점검 진행 방식, docs 수정 목록 표, 버린 대안 5건, 브랜치·커밋 단위, 기술 전제와 착수 실측이다. 사용자 확정이 필요한 값은 전제 3으로 비워 두고 Task 0 목록에 제안값과 함께 뒀다.

---

## Tasks

> AI가 순서대로 실행할 작업 단위를 정의합니다.
> **작성 원칙**: 구현자에게 이 문서 외 컨텍스트가 없다고 가정하고, Task별 완료 기준을 결정적으로 씁니다.
> 각 작업은 독립적으로 검증 가능해야 하며, 완료 기준은 **항목별 리스트로 쪼개 레벨 태그 + 검증 수단**을 적습니다.
> 명령으로 환원이 어려운 항목만 판정 주체(다른 AI / 사람)를 적고 강등 사유를 남깁니다.
>
> **대상 요구사항 필드** ― 일반 Task에는 `- **대상 요구사항**: R<n>` 필드를 `목표` 다음 행에 두어
> 이 Task가 spec의 어느 요구사항을 구현하는지 밝힙니다. 여러 개면 `R<n>, R<m>`로 나열합니다.
> 고정 Task(Task 0·Task N)는 특정 요구사항에 속하지 않으므로 이 필드를 두지 않습니다.
>
> **완료 기준 형식** ― 본문(접기 금지)에는 레벨 태그 + 무엇이 보장되는지 한 문장(+ 강등 사유)만 두고,
> 검증 명령·테스트케이스·기대 출력·명령 설계 주의점은 접기로 내립니다. 접기 제목에 기대 출력을 함께 적습니다
> (기본형: "검증 명령 ― 출력 0건이면 통과"). 접을 상세가 없는 항목은 접기 없이 본문 한 줄로 둡니다.
> 다른 게이트의 선행 조건이 되는 항목은 의존 관계와 **선행 조건이 무너졌을 때의 결과**를 본문 문장에 남기고, 메커니즘만 접기에 둡니다.
> 첫 고정 Task(Task 0 구현 시작 게이트)와 마지막 고정 Task(교차모델 issue-audit)는 삭제하지 말고 그대로 둡니다.
>
> **검증 레벨** ― 낮을수록 좋다(자동 검증에 가까움). 기본은 L1, 한 레벨 내릴 때마다 강등 사유를 함께 적는다.
>
> - `[D]`  L1 결정적   ― 명령이 합/불을 판정, 사람 판단 없음
> - `[QD]` L2 준결정적 ― 다른 AI·기준 체크리스트가 채점
> - `[ND]` L3 비결정적 ― 사람이 직접 읽고 판단
>
> **문서를 대상으로 검증할 때는 문구가 아니라 행 구조를 셉니다**. 같은 문구가 주석·안내문에도 등장하므로
> `grep -c '<문구>'`는 실제 구조가 없어도 통과합니다. 헤더는 `^## `, Task는 `^### Task `,
> 필드는 `^- \*\*…\*\*:`로 앵커를 고정하고, 명령은 작업 디렉토리에 의존하지 않게 repo 루트 기준 경로로 적습니다.
> **검증 명령 보정은 반례 격추가 아니라 불변식 전수 명세로 합니다**. 지적된 변형 하나만 막으면 이웃 변형이
> 다음 감사에서 재발하므로, 기대 구조를 열거하고 "그 외 0건"까지 판정에 넣습니다.
>
> **이 이슈의 구간 나누기**: 덱 93장을 착수 시점 번호(노트 부록 B-1) 기준으로 4구간으로 나눠 Task 3~6이 맡는다.
> Task 7(편입)이 장을 새로 넣으면 번호가 밀리므로, Task 7에서 점검표 번호를 다시 매기고 R5 게이트(행 수 = H1 수)로 정합을 확인한다.
> 구간 게이트의 번호 범위는 착수 시점 번호다.

### Task 0 (고정): 구현 시작 게이트 (전제·모호점 확인)

<!--
이 블록은 모든 이슈 계획의 첫 Task로 고정한다. 삭제하지 말 것.
설계 종료 게이트가 "쓰는 쪽"의 자기점검이라면, 이 게이트는 "읽는 쪽"이 착수 전에 거는 확인이다.
spec을 쓴 주체와 구현하는 주체가 같아도 수행한다 ― 세션이 바뀌면 전제는 똑같이 유실된다.
-->

- [x] 완료
- **목표**: 구현에 필요하지만 문서만으로는 알 수 없는 전제·모호점을 코드 작성 전에 걷어낸다.
- **작업 내용**:
  1. spec/plan을 읽고, 구현에 필요하지만 문서만으로는 알 수 없는 전제·모호점을 나열한다. 설계 시점에 이미 드러난 확정 대상은 아래 10건이다 (제안값은 질의의 출발점이며 사용자가 뒤집을 수 있다):
      1. **보안 정리표(91장, 3121~3143행)**: 이슈 본문의 두 갈래. (A) docs 정리표 13행 전부를 옮기고 폰트를 줄이거나 2단으로 나눔 / (B) 1부 8행을 유지하고 제목을 「1부 정리」로 한정, 외부 연결 5행은 78장이 맡는다는 한 줄을 붙임. 제안은 A다. 같은 이름·형식의 표가 다른 목록인 것이 드리프트이고, docs 표 라벨(「개인정보 가리고 입력하기(비식별 처리)」·「AI 결과물을 원본으로 검증」)도 함께 맞춘다. 화면에서 넘치면 그때 B로 물러선다.
      2. **편입할 docs 절**: spec 전제 4의 9건 중 고른다. 제안은 3건 편입이다. ⑧ 「확장 프로그램은 일반 프로그램과 같은 권한으로 내 컴퓨터에서 실행됩니다」(보안 성격, 70·72장 중 한 곳에 카드 추가), ⑤ 진행 원칙 ②(81장 카드에 한 줄 추가), ④ 하네스 3분류(18장에 한 줄 또는 카드 추가. 아래 9번과 묶음). 나머지 6건은 「요약이라 허용한 생략」으로 판정해 점검표 부록에 근거를 남긴다.
      3. **색 카드 정리 포함 여부**: 제안은 제외다 (spec 제외 항목의 근거). 포함으로 확정되면 Task 7과 Task 8 사이에 Task를 추가하고 spec 요구사항 R7을 신설한다.
      4. **「AI 활용을 배워야 하는 이유」 라벨 `①②③`(298·330·356행)**: docs는 번호 목록으로 바꿨다. 제안은 유지다. 슬라이드 라벨은 연속 3장의 순서 표시이고 docs 목록 순서와 같아 「다른 말」이 아니다.
      5. **슬라이드 전용 문장 903행** 「처음부터 본인의 챗봇이나 자동화 도구를 만드는 사람은 없습니다」: docs `index.md` 111행 「AI에게 매번 새로 시키기도 좋은 출발점입니다」가 같은 취지다. 제안은 docs 문장으로 교체(삭제 쪽)다. docs 편입은 docs 변경을 만들어 전제 11 표가 필요해진다.
      6. **발표자 노트 처리 방침**: 제안은 다음과 같다. 문구 게이트는 본문 한정(spec 전제 12(d)). 노트 중 본문과 모순되는 설계 근거(3192행 Projects 라벨 축약, 888·890행 「에이전트 스킬」 채택 근거, 2200행 옛 절 제목 파생 관계, 1211행 「시리즈 차원의 자세」)는 Task 8에서 현재 본문 기준으로 갱신하고, 이슈 번호·경위 서술은 보존한다.
      7. **66장(2173행) 제목·리드 방향**: docs `#vs-connector` 「커넥터와 MCP 서버는 같은 것입니다」. 제안은 제목을 「커넥터와 MCP 서버는 같은 것입니다」로 바꾸고 리드 첫 문장에 「같은 MCP 위에서 동작한다, 다른 점은 목록에 있느냐다」를 두는 것이다. 표·하단 안내는 docs 본문과 대조해 유지한다. 검토 표시 서술은 K-0001 재검토 조건이 걸리는 자리라 손댈 때 원장을 본다.
      8. **B-2 ④ 프롬프트 예시(39장, 1141·1153~1155행)**: 슬라이드는 보고서 3줄 요약, docs는 서울 날씨(step01과 연결). 제안은 docs 날씨 예시로 교체다. 요지는 같지만 docs 예시가 실습과 이어지는 자리라 다른 예시를 두면 청중이 실습에서 다시 만나는 연결이 끊긴다.
      9. **B-2 ⑥ 하네스 3분류 참조(56장 1764행)**: 2번에서 ④ 편입을 택하면 18장에 3분류가 생겨 1764행이 성립한다. 편입하지 않으면 1764행을 `basics.md` 44행 절의 요약(「1부에서 하네스를 내가 세팅하는 것들로 봤고, 외부 서비스 연결이 그중 하나였다」)으로 재작성한다. 제안은 편입이다.
      10. **점검표 파일과 구간·커밋 단위**: 점검표는 `.ai/90_issues/active/issue-0056/issue-0056-sync-check.md`(#52 선례 이름). 구간은 Task 3~6의 4구간, 커밋은 구간 승인마다 1회(spec 전제 13). 제안대로 확정한다.
  2. 항목이 있으면 **코드를 쓰기 전에 사용자에게 질의**하고, 답변을 spec `## 전제 (Assumptions)` 섹션(전제 3)에 반영한 뒤 구현을 시작한다. 제안값과 다르게 결정된 항목은 summary Task 0에 따로 적는다.
  3. 항목이 없으면 summary Task 0의 `수행 내용 요약`에 `전제 누락 없음` 한 줄을 기록하고 진행한다.
- **완료 기준**:
  - [ND] 나열한 항목이 전부 spec `## 전제 (Assumptions)`에 반영되어 미해소 0건이거나, summary Task 0에 `전제 누락 없음`이 기록된다  (검증: 사람 리뷰)  ← 강등 사유: 전제·모호점을 빠짐없이 나열했는지는 의미 판단이라 명령으로 환원 불가

---

### Task 1: 기준선 재측정과 점검표 골격

- [ ] 완료
- **목표**: 노트 매핑표를 현재 docs 구조에 맞게 다시 잡고, 93행 점검표 골격과 게이트 착수값을 만든다.
- **대상 요구사항**: R5
- **작업 내용**:
  1. 노트 부록 B-1 매핑표 93행의 docs 출처를 HEAD 기준으로 갱신한다. 줄 번호 대신 docs 헤딩 앵커(`{ #id }`)를 우선 쓰고, 앵커가 없는 절은 헤딩 문구를 적는다. #55로 절이 사라지거나 합쳐진 장은 비고에 적는다.
  2. #55 docs 커밋 20건(spec 전제 1)을 영향 장 번호에 매핑한 보조표를 점검표 부록으로 둔다. 커밋 하나가 여러 장에 걸치면 전부 적는다.
  3. `issue-0056-sync-check.md`를 만든다. 구성은 헤더(점검 기준·판정 정의·기준 커밋 SHA 행 `> 기준 커밋: \`<sha>\``)·93행 표(열은 `| # | 슬라이드 제목 | 판정 | 근거·대응 docs |`, 판정 열은 비움)·부록(B-3 9건 재확인 표, 커밋 매핑표)이다. 판정 정의는 #52 선례를 따른다. 반영은 슬라이드가 docs와 같은 내용(요약 허용, 이번 정정 포함), 해당 없음은 docs 대응 자체가 없음(구조 슬라이드·Task 0 확정 처리), 누락은 대응 docs가 있는데 다르거나 빠짐(작업 중 임시 값).
  4. spec DoD의 [D] 게이트 전부를 실행해 착수값을 summary Task 1에 기록한다 (R1·R2·R5·공통 4종).
- **완료 기준**:
  - [D] 점검표 행 수가 덱 H1 수와 같다 (착수 시점 93). 판정 열은 이 Task에서 비어 있어도 된다
    <details>
    <summary>검증 명령 ― 출력 0건이면 통과</summary>

    ```bash
    F=.ai/90_issues/active/issue-0056/issue-0056-sync-check.md
    N=$(grep -cE '^# ' slides/slides.md)
    R=$(grep -cE '^\| [0-9]+ \| ' "$F" 2>/dev/null); R=${R:-0}
    [ "$R" -eq "$N" ] || echo "위반: 점검표 ${R}행, 덱 ${N}장"
    ```

    </details>
  - [D] 점검표 헤더에 기준 커밋 행이 있고, 부록에 커밋 매핑표가 #55 docs 커밋 수(20)만큼 있다
    <details>
    <summary>검증 명령 ― 출력 0건이면 통과</summary>

    ```bash
    F=.ai/90_issues/active/issue-0056/issue-0056-sync-check.md
    grep -qE '^> 기준 커밋: `[0-9a-f]{7,}`' "$F" || echo '위반: 기준 커밋 행 없음'
    C=$(grep -cE '^\| [0-9a-f]{7,} \| ' "$F"); [ "${C:-0}" -eq 20 ] || echo "위반: 커밋 매핑 ${C:-0}행 (기대 20)"
    ```

    - 설계 주의: 커밋 행은 `| <sha7> | 제목 | 영향 장 |` 형식으로 시작해 슬라이드 행(`| <숫자> |`)과 앵커가 다르다. 20은 `git log --format=%h 3e57651..bfa5f67 -- docs labs/README.md mkdocs.yml .gitattributes | wc -l`의 착수 시점 값이다.
    </details>
  - [QD] 매핑이 docs 현재 구조와 맞다. Task 3~6 착수 시 구간마다 다시 확인하고 어긋나면 그 Task에서 고친다  (검증: 교차모델 audit이 표본 장의 출처 앵커 대 docs 헤딩 대조 채점)  ← 강등 사유: 대응 절 판단은 의미 판단이라 명령으로 환원 불가

---

### Task 2: 용어 통일 전수

- [ ] 완료
- **목표**: spec 전제 5의 대응표대로 덱 본문 용어를 한 번에 바꿔, 이후 구간 대조가 내용에만 집중하게 한다.
- **대상 요구사항**: R2, R6
- **작업 내용**:
  1. 「에이전트 스킬」 3곳(554·565·872행): 19장은 표 머리글 첫 등장을 「Agent Skills」, 카드(565행)를 「Skill」로. 29장 카드(872행)는 「Skill」로. 546행 제목 「1회성 · 스킬 · 애플리케이션」은 docs 제목대로 「1회성 결과물 · Skill · 애플리케이션」으로 하고 554행 표 머리글 「1회성 프롬프트」도 「1회성 결과물」로 바꾼다.
  2. 238행 「Team / Enterprise」를 「Team·Enterprise」로.
  3. 1174행 「시리즈 차원의 자세」를 「공통 원칙」으로. 1211행 노트는 Task 8 방침에 따라 처리한다.
  4. Projects 3곳: 1007행 2단계 도구 열에서 「Claude Projects」를 지우고 「Agent Skills 기초」만 남긴다. 3163행 링크를 지운다. 3192행 노트는 Task 8에서 갱신한다.
  5. dev 서버에서 사용자에게 19·29·9·40·33·92장 확인을 요청한다 (표 머리글이 길어진 19장과 카드 줄바꿈 이력이 있는 29장이 우선). 승인 후 `docs: 슬라이드 용어를 docs 확정 표기로 통일 (#56)`로 커밋한다.
- **완료 기준**:
  - [D] spec R2 [D] 게이트 통과 (덱 본문에서 옛 용어 5종 0건)
    <details>
    <summary>검증 명령 ― 출력 0건이면 통과</summary>

    ```bash
    sed '/^<!--$/,/^-->$/d' slides/slides.md \
      | grep -nE '에이전트 스킬|1회성 프롬프트|Team / Enterprise|시리즈 차원의 자세|Projects'
    ```

    </details>
  - [D] 장 첫 등장 규칙이 지켜진다. 「Skill」이 본문에 나오는 장(H1 단위)마다 그 장 안에서 「Agent Skills」가 「Skill」 단독 표기보다 먼저 나오거나, 그 장이 「Skill」 단독 표기를 쓰지 않는다
    <details>
    <summary>검증 명령 ― 출력 0건이면 통과</summary>

    ```bash
    sed '/^<!--$/,/^-->$/d' slides/slides.md | awk '
      /^# / { if (bad) print "위반: " title; title = $0; seen = 0; bad = 0 }
      /Agent Skills/ { seen = 1 }
      /Skill/ && !/Agent Skills/ && !seen { bad = 1 }
      END { if (bad) print "위반: " title }'
    ```

    - 설계 주의: 표지·목차처럼 「Skill」 단독 표기가 없는 장은 대상이 아니다. URL 안의 `skills` 소문자는 대소문자가 달라 걸리지 않는다. 한 행에 「Agent Skills」와 「Skill」이 같이 있으면 첫 등장으로 본다.
    </details>
  - [D] Slidev 빌드 통과  (검증: spec 공통 [D] 명령 재실행)
  - [ND] 사용자가 지정 장을 화면에서 확인하고 승인했으며 summary Task 2에 기록된다  (검증: 사람 리뷰)  ← 강등 사유: 시각 판정·승인은 사용자 행위

---

### Task 3: 1부 앞부분 동기화 (1~26장: Who·Why·기본 용어·도구와 제품)

- [ ] 완료
- **목표**: 구간 26장을 docs와 전수 대조해 판정·근거를 기록하고 드리프트를 정정한다.
- **대상 요구사항**: R1, R4, R5, R6
- **작업 내용**:
  1. 매핑표 기준으로 26장을 대응 docs 절과 대조하고 점검표 1~26행의 판정·근거를 채운다. 구조 슬라이드(1·2·3·11·16·21)는 「해당 없음」이다. 노트 B-6의 허용 생략(9·10·19·24·25·26장)은 「반영」에 생략 내용을 근거로 적는다.
  2. 정정 대상. 23장 677행 Cowork 정의를 docs `intro.md` 48행 「파일·도구를 직접 다루는 범용 에이전트」로 (B-2 ③). 24장 705행 외부 연결 행을 docs `intro.md` 66행 그대로 「외부 도구·데이터 연결 (커넥터·MCP 서버) \| ✓ 원격 · ✗ 로컬 \| ✓ 원격 · △ 로컬 \| ✓ 전체」로 (B-2 ②, #55 X10으로 claude.ai 로컬이 ✗). 18장은 Task 0 2번·9번 확정에 따라 3분류를 넣거나 대상 밖으로 둔다. 12~14장 라벨은 Task 0 4번 확정대로. 25장 740행 「헷갈리면 Sonnet → Opus → Fable」은 docs `intro.md` 모델 절과 대조해 요약 범위인지 확인한다.
  3. 대조 중 새로 드러난 드리프트는 점검표 근거에 적고 같은 Task에서 정정한다.
  4. dev 서버에서 사용자가 지정한 장을 확인하고 승인 후 `docs: 1부 앞부분(1~26장) 슬라이드 동기화 (#56)`로 커밋한다.
- **완료 기준**:
  - [D] 점검표 1~26행의 판정이 전부 반영 또는 해당 없음이다 (미판정·누락 0건)
    <details>
    <summary>검증 명령 ― 출력 0건이면 통과</summary>

    ```bash
    F=.ai/90_issues/active/issue-0056/issue-0056-sync-check.md
    grep -E '^\| [0-9]+ \| ' "$F" | awk -F'|' '{ n = $2 + 0; gsub(/^ +| +$/, "", $4)
      if (n >= 1 && n <= 26 && $4 !~ /^(반영|해당 없음)$/) print "위반: " $0 }'
    ```

    - 설계 주의: 번호 범위는 착수 시점 번호다. Task 7이 장을 넣어 번호를 다시 매기면 R5 전체 게이트가 대신 판정한다.
    </details>
  - [D] R1 문구 게이트 중 이 구간 몫(「데스크톱 범용 에이전트」·「MCP 연동」)이 0건
    <details>
    <summary>검증 명령 ― 출력 0건이면 통과</summary>

    ```bash
    sed '/^<!--$/,/^-->$/d' slides/slides.md | grep -nE '데스크톱 범용 에이전트|MCP 연동'
    ```

    </details>
  - [D] Slidev 빌드 통과  (검증: spec 공통 [D] 명령 재실행)
  - [QD] 26장이 대응 docs 절과 다른 말을 하지 않는다  (검증: 교차모델 audit이 점검표 근거 대 docs 대조 채점)  ← 강등 사유: 의미 대조
  - [ND] 사용자 화면 확인·승인 기록이 summary Task 3에 남는다  (검증: 사람 리뷰)  ← 강등 사유: 사용자 행위

---

### Task 4: 1부 뒷부분 동기화 (27~47장: What·How·트랙별 실습)

- [ ] 완료
- **목표**: 구간 21장을 docs `index.md` What·How와 전수 대조해 판정·근거를 기록하고 드리프트를 정정한다.
- **대상 요구사항**: R1, R4, R5, R6
- **작업 내용**:
  1. 매핑표 기준으로 27~47장을 대조하고 점검표 행을 채운다. 구조 슬라이드(27·36)는 「해당 없음」. 29장(결정 룰)은 #55가 docs `index.md` `#stage-model` 첫 부분에 편입했으므로 이제 대응 절이 있다. 29~31장을 그 편입분과 대조한다. B-6 허용 생략(44장)은 근거를 적는다.
  2. 정정 대상. 35장 1060행 학생·일반인 결과물 예시를 docs `index.md` 교육 목표 절의 표와 같게 (B-2 ①, 47장은 이미 일치). 30장 922행 「100번」을 docs 「여러 번」으로 (B-2 ⑧). 30장 903행 문장은 Task 0 5번 확정대로. 39장 프롬프트 예시는 Task 0 8번 확정대로 (B-2 ④). 40~45장 메타 원칙 5장은 docs `#meta-principles`와 대조하고, ③ 검토 의무 슬라이드는 #55 X3 확정(검토 깊이는 영향 범위에 비례, 외부 공유·개인정보는 항상 깊게)이 반영됐는지 본다. 32~34장 3단계 표·사다리·1단계 vs 2단계는 2단계 대표 도구가 Agent Skills로 통일됐는지 본다.
  3. 새로 드러난 드리프트는 같은 Task에서 정정한다.
  4. dev 서버 확인·승인 후 `docs: 1부 뒷부분(27~47장) 슬라이드 동기화 (#56)`로 커밋한다.
- **완료 기준**:
  - [D] 점검표 27~47행의 판정이 전부 반영 또는 해당 없음이다
    <details>
    <summary>검증 명령 ― 출력 0건이면 통과</summary>

    ```bash
    F=.ai/90_issues/active/issue-0056/issue-0056-sync-check.md
    grep -E '^\| [0-9]+ \| ' "$F" | awk -F'|' '{ n = $2 + 0; gsub(/^ +| +$/, "", $4)
      if (n >= 27 && n <= 47 && $4 !~ /^(반영|해당 없음)$/) print "위반: " $0 }'
    ```

    </details>
  - [D] R1 문구 게이트 중 이 구간 몫(「100번」·「AI 오답노트」)이 0건
    <details>
    <summary>검증 명령 ― 출력 0건이면 통과</summary>

    ```bash
    sed '/^<!--$/,/^-->$/d' slides/slides.md | grep -nE '100번|AI 오답노트'
    ```

    </details>
  - [D] Slidev 빌드 통과  (검증: spec 공통 [D] 명령 재실행)
  - [QD] 29~31장이 ADR-0005 메시지 3곳·결정 룰과 docs 편입분에 모순되지 않는다  (검증: 교차모델 audit이 ADR·docs 대조 채점)  ← 강등 사유: 의미 대조
  - [ND] 사용자 화면 확인·승인 기록이 summary Task 4에 남는다  (검증: 사람 리뷰)  ← 강등 사유: 사용자 행위

---

### Task 5: 2부 동기화 (48~79장: 도구·데이터 연결)

- [ ] 완료
- **목표**: 구간 32장을 `connect/` 3페이지와 `security-guide.md` 외부 연결 절과 전수 대조해 판정·근거를 기록하고 드리프트를 정정한다.
- **대상 요구사항**: R1, R4, R5, R6
- **작업 내용**:
  1. 매핑표 기준으로 48~79장을 대조하고 점검표 행을 채운다. 구조 슬라이드(48·49·55·65)는 「해당 없음」. B-6 허용 생략(54·62·73·74·75장)은 근거를 적는다. `mcp-servers.md`는 #55에서 재구성 폭이 컸으므로(spec 전제 2(e)) 66~75장은 절 단위가 아니라 문장 단위로 본다.
  2. 정정 대상. 66장 제목·리드는 Task 0 7번 확정대로 (spec 전제 2(c)). 56장 1764행 하네스 참조는 Task 0 9번 확정대로 (B-2 ⑥). 66·73장의 커넥터 검토 표시 서술을 손대면 K-0001 재검토 조건을 확인하고 원장에 재검토 이력을 남긴다. 53·54장은 `connect/index.md`, 57~64장은 `connectors.md`, 76~78장은 `security-guide.md` `#external-connection` 이하와 대조한다. 79장 「2부 정리」는 `mcp-servers.md` `#out-of-scope`·`#next`와 `connect/index.md` 읽는 순서 절과 대조한다.
  3. 새로 드러난 드리프트는 같은 Task에서 정정한다.
  4. dev 서버 확인·승인 후 `docs: 2부(48~79장) 슬라이드 동기화 (#56)`로 커밋한다.
- **완료 기준**:
  - [D] 점검표 48~79행의 판정이 전부 반영 또는 해당 없음이다
    <details>
    <summary>검증 명령 ― 출력 0건이면 통과</summary>

    ```bash
    F=.ai/90_issues/active/issue-0056/issue-0056-sync-check.md
    grep -E '^\| [0-9]+ \| ' "$F" | awk -F'|' '{ n = $2 + 0; gsub(/^ +| +$/, "", $4)
      if (n >= 48 && n <= 79 && $4 !~ /^(반영|해당 없음)$/) print "위반: " $0 }'
    ```

    </details>
  - [D] 66장 제목이 옛 절 제목 파생(「커넥터와 무엇이 다른가」)이 아니다
    <details>
    <summary>검증 명령 ― 출력 0건이면 통과</summary>

    ```bash
    grep -nE '^# 커넥터와 무엇이 다른가' slides/slides.md
    ```

    - 설계 주의: Task 0 7번에서 제목을 유지하기로 확정되면 이 항목을 지우고 그 사실을 summary에 적는다.
    </details>
  - [D] Slidev 빌드 통과  (검증: spec 공통 [D] 명령 재실행)
  - [QD] 66~75장이 재구성된 `mcp-servers.md`와 문장 단위로 다른 말을 하지 않는다  (검증: 교차모델 audit이 문장 대조 채점)  ← 강등 사유: 의미 대조
  - [ND] 사용자 화면 확인·승인 기록이 summary Task 5에 남는다  (검증: 사람 리뷰)  ← 강등 사유: 사용자 행위

---

### Task 6: 실습·보안·참고 동기화 (80~93장)

- [ ] 완료
- **목표**: 구간 14장을 `labs.md`·`security-guide.md`·`intro.md` 참고 절과 전수 대조해 판정·근거를 기록하고 드리프트를 정정한다.
- **대상 요구사항**: R1, R4, R5, R6
- **작업 내용**:
  1. 매핑표 기준으로 80~93장을 대조하고 점검표 행을 채운다. 구조 슬라이드(80·83·93)는 「해당 없음」. 82장 「step별 학습 포인트」는 ADR-0006상 `labs/*/README.md`가 SSoT라 `docs/labs.md`가 아니라 `labs/`와 대조한다. B-6 허용 생략(86장)은 근거를 적는다.
  2. 정정 대상. 80장 2840행 부제 「손으로 따라가는」을 진행 원칙 ①과 맞는 부제로 (B-2 ⑦). 81장 표는 docs `labs.md` 시리즈 구성 표와 같게 (step01 학습 포인트에 「데이터·뷰 분리」 추가, step03을 docs 표기 「이미지 입력, 출처 충실성 제약, 외부 사전 링크 자동 연결」로, 구분자·`데이터/뷰` 표기는 spec 전제 5의 별도 판단대로 사용자에게 확인). 81장 카드에 진행 원칙 ②는 Task 0 2번 확정대로. 91장 보안 정리표는 Task 0 1번 확정대로 (B-2 ⑨). 92장 참고 링크 3곳을 docs `intro.md` `#official-references`와 같게 (spec 전제 14: 「Anthropic Learn」→「Claude Academy」·MCP 안내 URL·플랜 URL). 84~90장은 `security-guide.md` 공통 원칙·트랙별 절과 대조한다.
  3. 새로 드러난 드리프트는 같은 Task에서 정정한다.
  4. dev 서버 확인·승인 후 `docs: 실습·보안·참고(80~93장) 슬라이드 동기화 (#56)`로 커밋한다.
- **완료 기준**:
  - [D] 점검표 80~93행의 판정이 전부 반영 또는 해당 없음이다
    <details>
    <summary>검증 명령 ― 출력 0건이면 통과</summary>

    ```bash
    F=.ai/90_issues/active/issue-0056/issue-0056-sync-check.md
    grep -E '^\| [0-9]+ \| ' "$F" | awk -F'|' '{ n = $2 + 0; gsub(/^ +| +$/, "", $4)
      if (n >= 80 && n <= 93 && $4 !~ /^(반영|해당 없음)$/) print "위반: " $0 }'
    ```

    </details>
  - [D] 92장 링크가 docs와 같은 URL 집합이다. 슬라이드 92장의 외부 URL이 docs `intro.md` `#official-references` 절의 URL 집합에 전부 포함된다
    <details>
    <summary>검증 명령 ― 출력 0건이면 통과</summary>

    ```bash
    D=$(awk '/^## 공식 문서·참고 자료/{f=1} f && /^## 함께 읽어보세요/{f=0} f' docs/intro.md | grep -oE 'https://[^ )]+' | sort -u)
    awk '/^# 공식 문서·참고 자료/{f=1} f && /^---$/{f=0} f' slides/slides.md | grep -oE 'https://[^ )]+' | sort -u \
      | while read -r u; do echo "$D" | grep -qxF "$u" || echo "위반: 슬라이드 링크 $u 가 docs 참고 절에 없음"; done
    ```

    - 설계 주의: 포함 관계만 본다 (docs가 더 많이 가진 것은 요약 허용). 슬라이드 92장 범위는 H1부터 다음 `---`까지다.
    </details>
  - [D] R1 문구 게이트 중 이 구간 몫(「손으로 따라가는」)이 0건
    <details>
    <summary>검증 명령 ― 출력 0건이면 통과</summary>

    ```bash
    sed '/^<!--$/,/^-->$/d' slides/slides.md | grep -nE '손으로 따라가는'
    ```

    </details>
  - [D] Slidev 빌드 통과  (검증: spec 공통 [D] 명령 재실행)
  - [ND] 사용자 화면 확인·승인 기록이 summary Task 6에 남는다  (검증: 사람 리뷰)  ← 강등 사유: 사용자 행위

---

### Task 7: 편입 절 신설 (Task 0 확정분)

- [ ] 완료
- **목표**: Task 0에서 편입으로 확정한 docs 절을 슬라이드로 파생하고 목차·점검표를 정합시킨다.
- **대상 요구사항**: R3, R5, R6
- **작업 내용**:
  1. 편입 절마다 구성안(넣을 위치·제목·카드 구성·발표자 노트 요지·기존 장에 얹을지 새 장을 만들지)을 표로 제시하고 사용자 승인을 받는다. 기존 장에 한 줄·카드로 얹는 편입은 새 장을 만들지 않는다.
  2. 승인된 구성안대로 작성한다. 새 장을 만들면 H1을 두고, 「오늘의 흐름」·「2부의 흐름」 목차에 영향이 있으면 함께 고친다.
  3. 새 장이 생기면 점검표에 행을 넣고 번호를 다시 매긴다. 판정은 「반영」, 근거에 편입 출처 절을 적는다.
  4. dev 서버 확인·승인 후 `docs: docs 누락 절 슬라이드 편입 (#56)`로 커밋한다.
  5. Task 0에서 편입 0건으로 확정되면 이 Task는 스킵하고 summary에 그 사실을 적는다.
- **완료 기준**:
  - [D] 점검표 행 수가 덱 H1 수와 같다 (새 장이 전부 점검표에 있고 번호 재부여가 끝남)
    <details>
    <summary>검증 명령 ― 출력 0건이면 통과</summary>

    ```bash
    F=.ai/90_issues/active/issue-0056/issue-0056-sync-check.md
    N=$(grep -cE '^# ' slides/slides.md)
    R=$(grep -cE '^\| [0-9]+ \| ' "$F" 2>/dev/null); R=${R:-0}
    [ "$R" -eq "$N" ] || echo "위반: 점검표 ${R}행, 덱 ${N}장"
    ```

    </details>
  - [D] Slidev 빌드 통과  (검증: spec 공통 [D] 명령 재실행)
  - [ND] 구성안 승인과 화면 확인 승인 기록이 summary Task 7에 남는다  (검증: 사람 리뷰)  ← 강등 사유: 사용자 행위
  - [QD] 편입 장이 출처 docs 절과 다른 말을 하지 않는다  (검증: 교차모델 audit)  ← 강등 사유: 의미 대조

---

### Task 8: 마감 (집계·발표자 노트 갱신·게이트 전수 재실행)

- [ ] 완료
- **목표**: 점검표를 집계하고, 본문과 모순되는 발표자 노트를 갱신하며, spec DoD의 [D] 게이트를 전부 재실행해 통과시킨다.
- **대상 요구사항**: R1, R2, R5
- **작업 내용**:
  1. 점검표에 집계 행을 넣는다. 형식은 `**집계**: 반영 N · 해당 없음 M · 누락 0 · 미판정 0`이며 N+M이 행 수와 같아야 한다.
  2. Task 0 6번 방침대로 발표자 노트를 갱신한다. 최소 대상은 3192행(Projects 라벨 축약 근거), 888·890행(「에이전트 스킬」 채택 근거), 2200행(옛 절 제목 파생 관계), 1211행(「시리즈 차원의 자세」)이다. 이슈 번호·경위 서술은 보존한다.
  3. spec DoD의 [D] 항목 전부를 재실행하고 체크한다. 실패가 있으면 해당 구간 Task로 돌아가 고치고 다시 실행한다.
  4. Task 0에서 색 카드 정리를 포함으로 확정했으면 이 Task 앞에 추가된 Task가 끝났는지 확인한다.
  5. summary의 `모델 기록` 표(설계·구현 모델)와 Task별 지표를 채우고 `docs: 점검표 집계와 발표자 노트 갱신 (#56)`로 커밋한다.
- **완료 기준**:
  - [D] 점검표 집계 행이 있고 반영·해당 없음 합이 행 수와 같다
    <details>
    <summary>검증 명령 ― 출력 0건이면 통과</summary>

    ```bash
    F=.ai/90_issues/active/issue-0056/issue-0056-sync-check.md
    L=$(grep -E '^\*\*집계\*\*: 반영 [0-9]+ · 해당 없음 [0-9]+ · 누락 0 · 미판정 0' "$F")
    [ -n "$L" ] || echo '위반: 집계 행 없음 또는 형식 불일치'
    S=$(echo "$L" | grep -oE '[0-9]+' | head -2 | paste -sd+ - | bc)
    R=$(grep -cE '^\| [0-9]+ \| ' "$F"); [ "${S:-0}" -eq "${R:-0}" ] || echo "위반: 집계 ${S:-0} ≠ 행 수 ${R:-0}"
    ```

    </details>
  - [D] spec DoD의 [D] 항목 8종 전부 통과  (검증: spec 각 명령 재실행, 결과를 summary Task 8에 기록)
  - [QD] 발표자 노트의 설계 근거가 현재 본문과 모순되지 않는다  (검증: 교차모델 audit이 갱신 대상 노트 5곳과 본문 대조 채점)  ← 강등 사유: 모순 여부는 의미 판단

---

### Task N (고정): 교차모델 issue-audit 검증 (사용자 수동 수행)

<!--
이 블록은 모든 이슈 계획의 마지막 Task로 고정한다. 삭제하지 말 것.
audit은 L2 [QD] 보완 검증 ― L1 [D] 결정적 게이트의 대체가 아니라 보완이다.
이 Task는 사용자가 직접 수행하며, 구현 AI는 자동으로 닫지 않는다.
-->

- [ ] 완료
- **목표**: 스펙 위반·누락·소스코드와의 모순을 구현 모델과 다른 시각으로 잡는다.
- **실행 주체**: **사용자가 직접** 수행한다. 구현 AI는 이 Task를 **자동으로 닫지 않으며**, `issue-audit`를 자동 실행하지도 않는다.
- **작업 내용**:
  1. spec `완료의 정의`의 `[D]` 항목 검증 명령을 전부 재실행해 통과를 확인한다.
  2. 구현을 수행한 모델과 **다른 벤더 모델**(최소 동급 이상 역량)로 사용자가 직접 `issue-audit`를 실행한다. 방향은 칭찬이 아니라 허점 탐색("스펙 위반·누락·소스코드와의 모순을 찾아라").
  3. 사용자가 audit 결과(지적 사항·감사 모델)를 summary에 반영·기록한다. 발견사항 보정은 issue-work `--response`로 검토한다. **피드백 먼저, 항목별 승인 후에만** 앞 Task를 보정하며, 리포트를 받자마자 자동 보정하지 않는다.
- **완료 기준**:
  - [D] spec `완료의 정의`의 `[D]` 항목 검증 명령 전부 통과  (검증: 해당 명령 재실행)
  <!-- 아래 5개 항목은 순서가 아니라 의존 관계다: 선행 조건(Task 집합 일치 → 결과 확정 → 수행 모델 채움)이 서지 않으면 교차 벤더 비교가 공집합이 되어 통과처럼 보인다. -->
  - [D] summary의 Task 헤더 집합이 plan과 일치한다. 블록 전체 누락·Task N 누락·빈 summary·두 경로 오기를 차단하며, 아래 두 게이트의 선행 조건이라 블록 자체가 없으면 그 두 게이트는 검사 대상이 사라져 그대로 통과한다
    <details>
    <summary>검증 명령 ― repo 루트에서 실행, 출력 0건이면 통과</summary>

    ```bash
    P=.ai/90_issues/active/issue-0056/issue-0056-plan.md
    S=.ai/90_issues/active/issue-0056/issue-0056-summary.md
    { grep -qE '^### Task ' "$P" && grep -qE '^### Task ' "$S" \
      && diff <(grep -E '^### Task ' "$P") <(grep -E '^### Task ' "$S") \
      || echo '위반: 입력 접근 실패 또는 Task 집합 불일치'; }
    ```

    - 설계 주의: 프로세스 치환 안의 `grep` 실패는 `diff` 종료 상태로 전파되지 않아 두 경로가 모두 잘못되면 빈 입력끼리 비교해 통과한다. 선행 `grep -q`로 두 파일의 Task 헤더 실재를 먼저 확인하고 실패를 stdout 위반 행으로 환원한다.
    - 의존 근거: 아래 두 게이트는 `^### Task ` 블록 안의 행만 훑는 awk라 블록 유무 자체를 판정할 수 없다. 그 판정은 이 게이트에만 있다.
    </details>
  - [D] 이 검증 전에 Task 0 및 모든 일반 실행 Task의 summary `결과`가 완료·부분 완료·스킵 중 하나로 확정된다. Task N 제외 블록마다 유효 `결과` 행 정확히 1개, 무효·중복 0건이며, 아래 `수행 모델` 게이트의 선행 조건이라 비워 두면 그 게이트의 검사 대상이 공집합이 되어 통과처럼 보인다
    <details>
    <summary>검증 명령 ― repo 루트에서 실행, 출력 0이면 통과</summary>

    ```bash
    S=.ai/90_issues/active/issue-0056/issue-0056-summary.md
    awk '
      /^### Task / { if (o && !n && v != 1) b++; o = 1; v = 0; n = ($0 ~ /^### Task N/) }
      o && /^- \*\*결과\*\*:/ {
        if ($0 ~ /^- \*\*결과\*\*: (완료|부분 완료|스킵)[[:space:]]*$/) v++
        else if (!n) b++
      }
      END { if (o && !n && v != 1) b++; print b+0 }
    ' "$S"
    ```

    - 설계 주의: 총개수 비교는 한 블록의 미확정을 다른 블록의 중복 행으로 상쇄해도 통과하므로 블록 단위로 센다.
    - 의존 근거: 아래 게이트의 "`수행 모델` 값 필수" 조건은 `결과`가 완료·부분 완료인 블록에서만 발동하도록 걸려 있다.
    </details>
  - [D] 완료·부분 완료 Task의 `수행 모델`이 비어 있지 않고 `-`도 아닌 행 정확히 1개(`-`는 미착수·스킵 전용)이며, 아래 교차 벤더 조건의 선행 조건이라 전부 `-`로 남기면 그 조건이 공집합이 되어 우회가 된다
    <details>
    <summary>검증 명령 ― repo 루트에서 실행, 출력 0이면 통과</summary>

    ```bash
    S=.ai/90_issues/active/issue-0056/issue-0056-summary.md
    awk '
      /^### Task / { if (o && !n && d && (t != 1 || m != 1)) b++; o = 1; d = 0; t = 0; m = 0; n = ($0 ~ /^### Task N/) }
      o && /^- \*\*결과\*\*: (완료|부분 완료)[[:space:]]*$/ { d = 1 }
      o && /^- \*\*수행 모델\*\*:/ { t++; if ($0 ~ /^- \*\*수행 모델\*\*: [^-[:space:]]/) m++ }
      END { if (o && !n && d && (t != 1 || m != 1)) b++; print b+0 }
    ' "$S"
    ```

    - 설계 주의: 리터럴 `-`만 거부하면 빈 값·행 누락·중복이 통과하므로 "없음"의 세 형태와 중복을 전부 실패로 센다.
    - 의존 근거: 아래 교차 벤더 조건은 비어 있지 않은 `수행 모델` 값에서만 벤더를 뽑아 대조한다.
    </details>
  - [QD] summary `모델 기록` 표의 `구현 모델`·`audit 모델` 두 행이 "벤더, 모델명" 형식으로 채워지고 서로 다른 벤더  (검증: 교차모델 audit이 두 행 대조 채점)  ← 강등 사유: 벤더 토큰 추출이 자유 문자열 의미 대조라 명령으로 환원하면 표기 변형에 취약하다
  - [QD] 구현에 여러 벤더가 관여했으면 audit 모델의 벤더가 Task 0 및 일반 실행 Task의 비어 있지 않은 모든 `수행 모델` 값에 나열된 벤더 전부와도 상이  (검증: 교차모델 audit이 `수행 모델` 행 전수 추출 후 audit 벤더와 대조 채점)  ← 강등 사유: 위와 같음. 표의 대표값 비교만으로는 audit 벤더의 구현 참여를 놓치고, 한 Task를 여러 모델이 수행했으면 그 값에 나열된 벤더를 모두 비교 대상에 넣는다
  - [ND] audit이 칭찬이 아니라 허점 탐색 방향으로 수행됨  (검증: 사용자가 audit 리포트 내용으로 판단)  ← 강등 사유: 감사 방향성은 리포트 서술의 의미 판단이라 명령으로 환원 불가
