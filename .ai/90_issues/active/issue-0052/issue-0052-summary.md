# Issue #52 실행요약 ― 2부(도구·데이터 연결) 슬라이드 신설

> 스펙: [issue-0052-spec.md](./issue-0052-spec.md) | 계획: [issue-0052-plan.md](./issue-0052-plan.md)

## 다음 작업

> ▶️ 다음 작업: Task 4 ― 1부 접합부 보정·목차 반영

## 모델 기록

<!--
이슈 단위 요약(대표값)이다. 세부 SSoT는 아래 `Task별 수행 결과`의 `수행 모델`이며,
한 구분에 여러 모델이 관여했으면 대표 모델을 적고 세부는 Task별 기록을 본다.

형식: "벤더, 모델명". 설계·구현·audit 어느 칸이든 어느 벤더·모델이든 가능하다
(예: Anthropic, Claude Opus 4.8 (claude-opus-4-8) / OpenAI, GPT-5.x / Google, Gemini 3.x).
설계 모델과 구현 모델은 다를 수 있어 칸을 나눈다 ― 같으면 같은 값을 적는다.
"audit 모델 ≠ 구현 모델" 조건을 나중에 명령으로 확인 가능하도록 기록한다. 모델 전환은 사람이 수행하므로 기계로 강제하기 어렵다.
구현에 여러 벤더가 관여했으면 audit 모델의 벤더는 Task별 `수행 모델`의 모든 벤더와도 달라야 한다
― 대표 행 비교만으로는 audit 벤더가 구현 일부에 참여한 경우를 놓친다.
-->

| 구분 | 모델 |
|------|------|
| 설계 모델 | Anthropic, Claude Fable 5 (claude-fable-5) |
| 구현 모델 | Anthropic, Claude Opus 5 (claude-opus-5[1m]) |
| audit 모델 | <!-- 구현 모델과 다른 벤더 모델. 형식: 벤더, 모델명. 마지막 교차모델 audit Task에서 사용자가 기록 --> |

---

## Task별 수행 결과

<!--
Task 0 및 일반 실행 Task(Task N 제외) 블록의 지표 4종은 사후 집계용이라 표기를 고정한다
(값이 없어도 필드는 지우지 않는다. Task N 예외의 근거는 아래 Task N 블록 주석).
- 수행 모델: "벤더, 모델명" ― `모델 기록` 표와 같은 형식. 한 Task를 모델 전환·재시도로 둘 이상이 수행했으면
  관여한 모델을 전부 ` / `로 나열한다 ― 하나만 남기면 누락된 벤더가 audit을 맡아도 Task N의 강화 조건을 통과한다.
  `-`는 미착수·스킵 Task 전용이며, `결과`가 완료·부분 완료면 실제 값을 필수로 기록한다
  (`-`로 남기면 Task N의 교차모델 강화 조건이 공집합이 되어 우회가 된다)
- audit 발견: 이 Task와 관련된 audit 발견사항 건수 ― 없으면 `0건` (`-` 금지)
- 보정 반영: `--response` 항목별 승인을 통과해 실제로 보정한 건수 ― 없으면 `0건` (`-` 금지)
- 재시도: 같은 Task에서 재수정·재시도한 횟수 ― 없으면 `0회` (`-` 금지)

한 발견이 여러 Task 산출물에 걸쳐도 가장 직접 수정된 주 Task 하나에만 귀속한다 ―
`audit 발견`·`보정 반영` 모두 그 주 Task에서 1회만 세고 관련된 다른 Task에는 세지 않는다.
Task 축에서도 이슈 단위 합계가 중복으로 부풀지 않게 하기 위함이다(아래 회차 축 중복 제외와 같은 취지).

수치 3종은 회차별 값이 아니라 이슈 전체 누적이다 ― 여러 차수 감사가 같은 발견을 재확인해도
1건으로 세고(중복 제외), 새 회차 값으로 덮어쓰지 않는다. 보정률 사후 집계의 분모·분자가 회차 반복으로 부풀지 않게 하기 위함이다.

수치 3종에 `-`를 쓰지 않는 이유: 아래 스니펫이 `[0-9]+`만 추출하므로 `-`인 Task는
합계에서 조용히 빠지고, 전부 `-`면 합계 자체가 빈 문자열이 된다. `-` 허용은 비수치 필드(`수행 모델`)에만 둔다.

수치 3종의 기본값은 주석이 아니라 리터럴 `0건`/`0회`로 둔다: 주석 placeholder는 렌더링 값이
비어 있는데도 집계기가 주석 안 숫자를 실제 값으로 오인하고, `미기록` 같은 형식 위반도 걸러지지 않는다.

보정률은 별도 필드로 두지 않고 `보정 반영 / audit 발견`으로 사후 집계한다.
아래 스니펫이 동작하도록 `- **<필드>**: <숫자><단위>` 표기를 지킨다 (줄 시작, 굵게, 콜론 뒤 숫자).

  # 이슈 하나의 보정률
  found=$(grep -E '^- \*\*audit 발견\*\*:' issue-0052-summary.md | grep -oE '[0-9]+' | paste -sd+ - | bc)
  fixed=$(grep -E '^- \*\*보정 반영\*\*:' issue-0052-summary.md | grep -oE '[0-9]+' | paste -sd+ - | bc)
  echo "보정률: ${fixed}/${found}"

  # 모델별 집계는 `수행 모델` 행과 함께 뽑는다
  grep -E '^- \*\*(수행 모델|audit 발견|보정 반영|재시도)\*\*:' issue-0052-summary.md
-->

### Task 0 (고정): 구현 시작 게이트 (전제·모호점 확인)

<!--
plan 템플릿의 첫 고정 Task에 대응하는 블록이다. 삭제하지 말 것.
질의로 해소한 전제는 spec `## 전제 (Assumptions)`에 남기고, 여기에는 수행 사실만 적는다.
나열한 항목이 없었으면 `수행 내용 요약`에 `전제 누락 없음` 한 줄을 기록한다.
-->

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Fable 5 (claude-fable-5)
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**: 착수 전 질의 3건(덱 구성 · 분량 목표 · 목차 처리)을 사용자 질의로 확정해 spec 전제 7·8에 반영했다. 덱 구성은 기존 덱에 이어 붙이기, 분량은 Task 1 구성안에서 제안 후 조정, 목차는 「오늘의 흐름」에 2부 구간 추가다.
- **특이 사항**: 이슈 본문 참고 3(#50의 slides 줄표 정리 결정 확인)은 spec 전제 5로 해소했다(#50에서 정리 완료). 설계 종료 게이트 발견 6건은 spec 전제 1~6에 기록돼 있다.

---

### Task 1: 2부 슬라이드 구성안 작성·승인

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Opus 5 (claude-opus-5[1m])
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 1회
- **수행 내용 요약**: 파생 원본 4곳과 기존 덱 관례를 조사해 장별 구성안을 `.ai/99_workspace/issue-0052-storyboard.md`에 작성했다(승인 후 175행). 사용자 확정 3건: ① 2부 블록을 **1부 「실습」 섹션 divider 바로 앞**에 배치, ② 분량 **31장**(초안 30장 + 갈림길 장 분할 1장), ③ 장별 사전 검토 대신 **31장 완성 후 일괄 피드백**(Task 6 루프). 확정 장 제목 31개를 plan Task 2(1~16장)·Task 3(17~31장) 검증 명령에 기입하고, 두 명령의 부정·긍정 케이스를 실측해 동작을 확인했다.
- **특이 사항**: 초안의 배치 권장(덱 맨 끝)을 사용자 지적으로 철회했다. 실습 섹션이 hands-on 진입 분기점이라 그 뒤에 강의 블록을 두면 순서가 어긋나고, 2부 실습이 신설되면 같은 실습 섹션에 합류하므로 「강의 전체 → 실습 전체」 구조가 맞다. mkdocs nav는 1부 실습 → 2부 순이지만 ADR-0002 결정 2가 슬라이드를 「발표 호흡에 맞게 재구성한 결과물」로 규정했고, 덱에 이미 선례가 있다(「기본 용어와 범위」·「도구와 제품」이 docs 위계와 달리 Why와 What 사이에 배치). 배치 변경의 부수 효과로 2부 끝의 `layout: end` 슬라이드를 제거했다.
  조사 중 Task 5 후보 2건을 발견했다(이번 Task에서 손대지 않음): 「실습 시리즈」 슬라이드의 step03 학습 포인트가 「사진 입력·출처 충실성·외부 도구 연결」인데 `docs/labs.md`는 「Skill 활용 다양화」이고, step04는 슬라이드 「검수 자리」 대 docs 「검수 단계」다.
  덱 관례 조사에서 **슬라이드 전체에 이미지가 0건**임을 확인해, docs의 커넥터 화면 이미지 5장을 가져오지 않고 화면 경로를 텍스트로 쓰기로 했다(plan Task 2 작업 내용 4에 기입).

---

### Task 2: 개요·기본 커넥터 슬라이드 작성

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Opus 5 (claude-opus-5[1m])
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**: 구성안 1~16장을 `slides/slides.md`에 작성해 삽입했다(1부 「실습」 섹션 divider 바로 앞, 666행 추가). 구성은 진입 2장(2부 divider · 2부의 흐름) + 개요 5장(`docs/connect/index.md` 파생) + 기본 커넥터 divider 1장 + 본문 8장(`docs/connect/connectors.md` 파생)이다. 덱 전체 제목 수는 61 → 77개가 됐다. 시각 관례는 기존 덱을 그대로 따랐다: `layout: section` divider, 카드 그리드, 좌측 컬러 바, 표 축소용 `<style>` 블록, 슬라이드별 HTML 주석(원본 매핑 + 설계 근거). `[D]` 게이트 4종(배치 · 앞 16장 제목·순서 · Slidev 빌드 · U+2014 0건) 전부 통과를 실행으로 확인했다.
- **특이 사항**: 색 배정을 1부 어휘와 맞춰 재사용했다. 3단계 대비 카드는 메타 원칙 ①과 같은 cyan, 연결 계열은 teal, 권장/보수 대비는 메타 원칙 ⑤의 emerald/amber, 메타 원칙 ③ 호출은 purple, 범위 밖 안내는 gray다. 청중이 1부에서 익힌 색 신호를 2부에서 다시 읽게 하는 것이 목적이다.
  docs 대비 의도적으로 줄인 곳이 셋 있다(모두 상세는 사이트로 위임). ① 6장의 방법 비교 표에서 docs 5행 중 「자세히 보기」 행(페이지 링크)을 뺐다. ② 14장 Atlassian 예시 프롬프트를 docs 3개씩에서 2개씩으로 줄였다. ③ 15장 GitHub에서 docs의 지원 범위·프로젝트에 저장소 붙이기·SSO 승인 상세를 뺐다. 연결하기로 정한 사람만 필요한 내용이라 판단 기준만 남겼다.
  16장은 docs 두 절(`#manage-disconnect`·`#enterprise`)을 한 장에 병합했다. 구성안에서 ⊗(늘릴 때 쪼갤 후보)로 표시된 장이며, 왼쪽은 개인이 하는 점검·오른쪽은 조직이 정하는 경계로 갈라 한 화면에서 대비되게 했다.
  화면 라벨은 실제 표기 그대로 유지했다(`사용자 지정 → 커넥터`, `도구 액세스`, `자동`·`항상 사용 가능`·`필요할 때`, `연결`·`설치`). 스크린샷은 덱 관례대로 0건이고 이미지 5장은 docs에 위임했다.
  Task 6 사용자 viewport 점검 대상 후보: 13장(카드 2 + 본문 + 카드 2로 요소가 가장 많음), 12장·6장(표 + 하단 서술).

---

### Task 3: MCP 서버·보안 유의점 슬라이드 작성

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Opus 5 (claude-opus-5[1m])
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**: 구성안 17~31장을 16장 뒤에 이어 작성했다(+679행). 구성은 MCP 서버 divider 1장 + 본문 10장(`docs/connect/mcp-servers.md` 파생) + 연결 보안 3장(`docs/security-guide.md#external-connection` 파생) + 정리 1장이다. 덱 전체 제목 수는 77 → 92개, 파일은 3181행이 됐다. 31장 뒤에 `layout: end` 슬라이드를 두지 않았고(구성안 2절), 다음 슬라이드는 기존 1부 「실습」 섹션 divider다. `[D]` 게이트 4종(31장 전수 제목·순서 · 배치 · Slidev 빌드 · U+2014 0건) 전부 통과를 실행으로 확인했다.
- **특이 사항**: ADR-0010 결정 3-1의 두 갈래를 19·20장으로 나눠 유지했다. 19장은 「설정을 어디에 두나」(접속 주체를 정하는 갈래), 20장은 「서버가 어디에서 실행되나」(설정 위치와 독립)이며, 20장에서 두 조합에 A·B 라벨을 붙여 22장 mermaid에서 회수한다. ADR이 이 갈래를 뒤늦게 추가한 이유(원격/로컬 한 갈래로 누르면 사내망 케이스가 빠짐)를 20장 발표자 노트에 남겼다. 슬라이드 본문에 「축」이라는 말은 0건이고(검증 실행), 발표자 노트에서만 ADR 서술을 참조한다.
  mermaid 2장(21·22장)은 docs 원본의 노드·간선·색 지정을 그대로 가져왔다. 덱 mermaid가 `setup/mermaid.ts`에서 neutral(라이트) 고정이라 docs의 연한 파랑 style 지정이 그대로 산다. 22장에서만 노드 라벨의 「사내 데이터」 이탤릭 한 줄을 뺐다(슬라이드 폭에서 노드가 3행이 되면 그림이 눌림). scale은 21장 0.72, 22장 0.62로 잡았고 **둘 다 Task 6 viewport 점검 대상**이다.
  구성안의 병합 표시를 적용해 두 장을 묶었다. 26장은 docs 세 박스(열쇠 값 발급 · 열쇠 값을 대화창에 넣지 않기 · 「항상 허용」)를 「승인을 미리 좁히기 vs 승인을 나중에 열기」 갈래로 한 장에, 27장은 `#after-connect`·`#troubleshooting` 두 절을 「잘 될 때 / 안 될 때」로 한 장에 묶었다.
  docs 대비 뺀 상세는 넷이다. ① 24장에서 고급 설정·MCPB 파일 배포·확장 프로그램 권한 경고, ② 27장 문제 해결 표의 「주소를 잘못 넣음」 행(24장 하단에서 이미 다룸), ③ 30장 임직원 카드의 GitHub Enterprise 판단(15장에서 이미 다룸), ④ 29장의 「절대 입력하면 안 되는 정보」 표(1부 보안 섹션에 이미 있음). 모두 중복 회수이거나 실제 등록자만 필요한 상세다.
  보안 3장(28~30)은 요약만 담고 기준을 다시 정의하지 않았다(ADR-0010 결정 4, SSoT는 `docs/security-guide.md`). 28장 하단에 사이트 페이지 안내를 남겼다.
  메타 원칙 ③은 2부에서 세 번 호출된다(5장 · 14장 · 27장). 세 자리 모두 purple 계열로 색을 맞춰 청중이 같은 원칙의 반복임을 색으로 읽게 했다.

---

### Task 4: 1부 접합부 보정·목차 반영

- **결과**: <!-- 완료 / 부분 완료 / 스킵 -->
- **수행 모델**: -
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**:
- **특이 사항**:

---

### Task 5: 1부 docs↔slides 동기화 점검 (#50 변경 이력 기준)

- **결과**: <!-- 완료 / 부분 완료 / 스킵 -->
- **수행 모델**: -
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**:
- **특이 사항**:

---

### Task 6: 사용자 점검·피드백 루프

- **결과**: <!-- 완료 / 부분 완료 / 스킵 -->
- **수행 모델**: -
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**:
- **특이 사항**:

---

### Task N (고정): 교차모델 issue-audit 검증 (사용자 수동 수행)

<!--
plan 템플릿의 마지막 고정 Task에 대응하는 블록이다. 삭제하지 말 것.
사용자가 직접 수행하며, 구현 AI는 이 블록을 대신 채우지 않는다.
지표 4종을 두지 않는다 ― 수행 모델은 위 `모델 기록` 표의 `audit 모델` 행이 SSoT이고,
audit 발견·보정 반영은 이 Task가 만들어낸 값이라 각 대상 Task 블록에 집계된다.
-->

- **결과**: <!-- 완료 / 부분 완료 / 스킵 -->
- **수행 내용 요약**: <!-- audit 리포트 위치, 발견사항 건수, `--response` 검토 결과 -->
- **특이 사항**:
