# Issue #49 실행계획 — 2부(심화편) 신설: MCP 연결 교육 자료

> 스펙: [issue-0049-spec.md](./issue-0049-spec.md)

---

## 설계 종료 게이트 (고정)

> **점검 질문**: 이 spec/plan만 보고, 작성에 참여하지 않은 쪽이 구현에 필요한 내용을 스스로 알 수 있는가?

- [x] 점검 완료
- **점검 대상** (작성 중 머릿속에만 있었던 것):
  - 코드베이스 관례 — 이 repo에서만 통하는 패턴·명명·배치 규칙
  - 버전·환경 제약 — 특정 버전·플랫폼·도구에 묶인 조건
  - 검토 후 버린 대안과 그 이유
  - 사용자와의 합의로만 정해진 값
- **발견 시 조치**: spec `## 전제 (Assumptions)` 섹션에 적는다. 발견이 없으면 그 섹션에 "없음" 한 줄을 남긴다.
- **점검 결과**: 대화 합의사항(2부 분리·같은 사이트·접합부 최소화), 버린 대안 2건, 작성자 제안(디렉토리명·부 명칭·nav 재편 방식·보안 배치·후속 이슈 분리), 도구 기준(비개발자 UI), 링크 규칙(/ko/)을 spec 전제에 기록했다.

---

## Tasks

> AI가 순서대로 실행할 작업 단위를 정의합니다.
> **작성 원칙**: 구현자에게 이 문서 외 컨텍스트가 없다고 가정하고, Task별 완료 기준을 결정적으로 씁니다.
>
> **검증 레벨** — 낮을수록 좋다(자동 검증에 가까움). 기본은 L1, 한 레벨 내릴 때마다 강등 사유를 함께 적는다.
>
> - `[D]`  L1 결정적   — 명령이 합/불을 판정, 사람 판단 없음
> - `[QD]` L2 준결정적 — 다른 AI·기준 체크리스트가 채점
> - `[ND]` L3 비결정적 — 사람이 직접 읽고 판단
>
> 검증 명령은 모두 repo 루트 기준 경로로 적는다.

### Task 0 (고정): 구현 시작 게이트 — 전제·모호점 확인

- [x] 완료
- **목표**: 구현에 필요하지만 문서만으로는 알 수 없는 전제·모호점을 코드 작성 전에 걷어낸다.
- **작업 내용**:
  1. spec/plan을 읽고, 구현에 필요하지만 문서만으로는 알 수 없는 전제·모호점을 나열한다.
  2. spec 전제의 **작성자 제안 5건**(디렉토리·파일명, 부 명칭, nav 재편 방식, 보안 배치, labs·slides 후속 분리)을 사용자에게 질의해 확정하고, 답변을 spec `## 전제 (Assumptions)`에 반영한 뒤 구현을 시작한다.
  3. 추가 항목이 없으면 summary Task 0의 `수행 내용 요약`에 확정 결과를 기록하고 진행한다.
- **완료 기준**:
  - [ND] 나열한 항목이 전부 spec `## 전제 (Assumptions)`에 반영되어 미해소 0건  (검증: 사람 리뷰)  ← 강등 사유: 전제·모호점을 빠짐없이 나열했는지는 의미 판단이라 명령으로 환원 불가

---

### Task 1: ADR-0010 작성 — 2부 신설·범위 확장 결정

- [x] 완료
- **목표**: 1부의 "Claude 단독·MCP 범위 밖" 선언(ADR-0001)을 확장하는 2부 신설 결정을 기록한다.
- **작업 내용**:
  1. `.ai/50_adr/active/adr-0010-<slug>.md` 작성 — 배경(통합 vs 별도 2부 비교), 결정(같은 repo·같은 사이트, nav 부 구분, 접합부 최소 수정), ADR-0001과의 관계(대체가 아니라 확장 — 1부 범위 선언은 유지, 2부가 새 범위를 추가).
  2. **도구 축 경계 명시**(Task 0 확정) — 2부는 ADR-0001의 도구 범위(Claude 단독)를 유지한 채 범위만 확장하며, 도구 다원화(ChatGPT 등)는 이 ADR의 결정 대상이 아니라 후속 결정에 위임함을 본문에 남긴다.
  3. `.ai/50_adr/index.md`의 Active ADR 표에 등재.
- **완료 기준**:
  - [D] ADR 파일 정확히 1개 존재  (검증: `ls .ai/50_adr/active/adr-0010-*.md | wc -l` 출력 1)
  - [D] 인덱스 표에 행 등재  (검증: `grep -cE '^\| \[ADR-0010\]' .ai/50_adr/index.md` 출력 1)
  - [QD] ADR 본문이 spec의 배경·결정·버린 대안과 모순 없음  (검증: 다른 AI가 채점, 별도 세션)  ← 강등 사유: 문서 간 의미 대조는 명령으로 환원 불가

---

### Task 2: 2부 골격 — docs/connect/ 3페이지 스텁 + nav 부 구분

- [x] 완료
- **목표**: 2부의 자리(디렉토리·nav)를 만들고 빌드가 통과하는 골격을 세운다.
- **작업 내용**:
  1. `docs/connect/index.md`·`connectors.md`·`mcp-servers.md` 스텁 생성 (H1 제목 + 개요 1~2문단).
  2. `mkdocs.yml` nav 재편(Task 0 확정) — 기존 본문 3페이지를 `1부 — AI 활용 기본` 섹션으로 묶고, 그 아래 `2부 — 도구·데이터 연결` 섹션에 2부 3페이지를 등재한다. 홈·보안 가이드·운영 가이드는 부 공통이므로 최상위에 그대로 둔다.
  3. 빌드 확인.
- **완료 기준**:
  - [D] 페이지 3종 존재·각 H1 정확히 1개  (검증: `for f in docs/connect/index.md docs/connect/connectors.md docs/connect/mcp-servers.md; do test -f "$f" && test "$(grep -c '^# ' "$f")" -eq 1 || echo "위반: $f"; done` 출력 0건)
  - [D] nav 등재  (검증: `grep -c 'connect/' mkdocs.yml` ≥ 3)
  - [D] 빌드 통과  (검증: `mkdocs build` 종료 코드 0)

---

### Task 3: 2부 개요 페이지 본문 — 연결이 왜 필요한가

- [x] 완료
- **목표**: 1부 도착점(에이전트 활용)에서 2부(외부 연결)로 이어지는 서사를 세운다.
- **작업 내용**:
  1. `docs/connect/index.md` 본문 작성 — 1부 3단계 모델과의 관계(새 단계가 아니라 도달한 에이전트의 손이 닿는 범위를 넓히는 축), 기본 커넥터 vs MCP 서버 두 갈래 미리보기, 준비물(플랜·연결 권한 등 — "플랜" 용어 통일).
  2. 외부 컨텍스트·MCP·커넥터 등 용어는 `docs/basics.md`의 기존 정의를 링크로 재사용하고 재정의하지 않는다.
- **완료 기준**:
  - [D] basics.md 정의 재사용 링크 존재  (검증: `grep -c 'basics.md' docs/connect/index.md` ≥ 1)
  - [QD] 비개발자 톤·1부 용어 정합, 3단계 모델과의 관계 서술이 ADR-0001과 모순 없음  (검증: 다른 AI가 채점, 별도 세션)  ← 강등 사유: 톤·정합은 의미 판단

---

### Task 4: 기본 커넥터 페이지 본문 — GitHub·Atlassian

- [x] 완료
- **목표**: 설정 파일 없이 UI에서 로그인·승인으로 잇는 기본 커넥터 사용법을 안내한다.
- **작업 내용**:
  1. `docs/connect/connectors.md` 본문 작성 — 커넥터 개념(`basics.md`의 "고르고·연결만 하는 것" 재사용), 연결·해제 절차, 승인 화면에서 확인할 것.
  2. GitHub 커넥터 예시(예: repo 문서·이슈 읽기), Atlassian 커넥터 예시(Jira 이슈 조회, Confluence 문서 읽기·작성) — 비개발자 업무 시나리오 중심.
  3. 공식 문서 링크는 `/ko/` 경로 우선.
- **완료 기준**:
  - [D] 두 커넥터 서술 존재  (검증: `grep -ci 'github' docs/connect/connectors.md` ≥ 1 그리고 `grep -ciE 'atlassian|jira|confluence' docs/connect/connectors.md` ≥ 1)
  - [D] 빌드 통과  (검증: `mkdocs build` 종료 코드 0)
  - [QD] 절차가 현재 제품 UI와 어긋나지 않음  (검증: 다른 AI가 공식 문서 대조 채점, 별도 세션)  ← 강등 사유: 외부 제품 UI 정확성은 명령으로 환원 불가

---

### Task 5: MCP 서버 페이지 본문 — 커넥터 너머

- [ ] 완료
- **목표**: 기본 커넥터에 없는 도구·데이터를 MCP 서버로 잇는 방법을 비개발자 눈높이로 안내한다.
- **작업 내용**:
  1. `docs/connect/mcp-servers.md` 본문 작성 — 커넥터와의 관계(커넥터는 관리형, MCP 서버는 직접 등록), Claude Desktop(Cowork) 기준 등록 절차, 신뢰할 서버 고르기, 안 될 때 확인할 것.
  2. 서버 "개발"은 범위 밖임을 명시하고 개발자용 문서는 포인터만 남긴다.
  3. `connectors.md`와 상호 링크로 두 갈래의 선택 기준을 잇는다.
- **완료 기준**:
  - [D] 상호 링크 존재  (검증: `grep -c 'connectors.md' docs/connect/mcp-servers.md` ≥ 1 그리고 `grep -c 'mcp-servers.md' docs/connect/connectors.md` ≥ 1)
  - [D] 빌드 통과  (검증: `mkdocs build` 종료 코드 0)
  - [QD] 비개발자 톤 유지, "이용까지만" 경계가 ADR-0005와 모순 없음  (검증: 다른 AI가 채점, 별도 세션)  ← 강등 사유: 톤·경계는 의미 판단

---

### Task 6: 연결 보안·주의사항 반영

- [ ] 완료
- **목표**: 외부 연결로 새로 생기는 데이터 이동 경로에 대한 보안 안내를 더한다.
- **작업 내용**:
  1. Task 0 확정 — **양쪽 배치**. 정책 본문은 `docs/security-guide.md`에 "외부 연결" 절을 신설해 쓴다 (연결 승인 시 확인할 권한 범위, 회사 데이터가 외부 서비스로 나가는 경로, 회사 정책·승인 절차 확인 안내). 보안 서술의 SSoT는 `security-guide.md`.
  2. 2부 각 페이지에는 그 자리에서 필요한 주의만 짧은 admonition으로 두고, 상세는 `security-guide.md` 해당 절로 링크한다 (본문 중복 금지).
  3. 금지·제약 표현은 예시 열거로 좁히지 않고 포괄 표현으로 쓴다.
- **완료 기준**:
  - [D] 보안 안내와 기존 가이드 연결 존재  (검증: `grep -rc 'security-guide.md' docs/connect/` 합계 ≥ 1 또는 `grep -c 'connect/' docs/security-guide.md` ≥ 1)
  - [QD] 기존 보안 가이드와 모순 없음  (검증: 다른 AI가 채점, 별도 세션)  ← 강등 사유: 정책 간 의미 대조는 명령으로 환원 불가

---

### Task 7: 1부 접합부 갱신 — basics.md·intro.md

- [ ] 완료
- **목표**: 1부의 "범위 밖" 선언을 2부 포워드로 전환하고 1부 서술 정합을 유지한다.
- **작업 내용**:
  1. `docs/basics.md`의 "외부 컨텍스트 — MCP·RAG" 박스에서 "본 강의의 범위 밖입니다" 선언을 "2부(심화편)에서 다룹니다" 포워드 링크로 교체. 정의 문장(MCP·RAG가 무엇인지)은 유지 — 정의의 자리는 basics.md다.
  2. `docs/intro.md`의 MCP 관련 서술(비교표 행, 개발자용 MCP 문서 링크)을 2부와 정합하게 점검·갱신.
  3. 1부 전반에서 "MCP는 범위 밖" 취지의 잔존 표현을 스캔해 정리.
- **완료 기준**:
  - [D] 2부 포워드 링크 존재  (검증: `grep -c 'connect/' docs/basics.md` ≥ 1)
  - [D] 범위 밖 선언 제거  (검증: `grep -c '본 강의의 범위 밖' docs/basics.md` 출력 0)
  - [D] 빌드 통과  (검증: `mkdocs build` 종료 코드 0)
  - [QD] 1부 나머지 서술(3단계 모델·도구 표)과 모순 없음  (검증: 다른 AI가 채점, 별도 세션)  ← 강등 사유: 문서 전반 정합은 의미 판단

---

### Task 8: 사용자 점검·피드백 루프

- [ ] 완료
- **목표**: 초안 일괄 반영으로 끝내지 않고, 사용자 직접 점검 → 피드백 → 조율을 반복해 마감한다.
- **작업 내용**:
  1. `mkdocs serve`로 로컬 확인 경로를 안내하고, 2부 3페이지 + 1부 접합부를 페이지 단위로 점검받는다.
  2. 피드백을 반영하고, 방향이 갈리는 항목은 조율 후 재반영한다.
- **완료 기준**:
  - [ND] 사용자가 2부 3페이지와 1부 접합부를 검토하고 반영 결과를 승인  (검증: 사람 리뷰)  ← 강등 사유: 교육 콘텐츠 적합성은 최종적으로 저자(사용자) 판단

---

### Task N (고정): 교차모델 issue-audit 검증 — 사용자 수동 수행

- [ ] 완료
- **목표**: 스펙 위반·누락·소스코드와의 모순을 구현 모델과 다른 시각으로 잡는다.
- **실행 주체**: **사용자가 직접** 수행한다. 구현 AI는 이 Task를 **자동으로 닫지 않으며**, `issue-audit`를 자동 실행하지도 않는다.
- **작업 내용**:
  1. spec `완료의 정의`의 `[D]` 항목 검증 명령을 전부 재실행해 통과를 확인한다.
  2. 구현을 수행한 모델과 **다른 벤더 모델**(최소 동급 이상 역량)로 사용자가 직접 `issue-audit`를 실행한다. 방향은 칭찬이 아니라 허점 탐색("스펙 위반·누락·소스코드와의 모순을 찾아라").
  3. 사용자가 audit 결과(지적 사항·감사 모델)를 summary에 반영·기록한다. 발견사항 보정은 issue-work `--response`로 검토한다 — **피드백 먼저, 항목별 승인 후에만** 앞 Task를 보정하며, 리포트를 받자마자 자동 보정하지 않는다.
- **완료 기준**:
  - [D] spec `완료의 정의`의 `[D]` 항목 검증 명령 전부 통과  (검증: 해당 명령 재실행)
  <!-- 아래 5개 항목은 순서가 아니라 의존 관계다: 선행 조건(Task 집합 일치 → 결과 확정 → 수행 모델 채움)이 서지 않으면 교차 벤더 비교가 공집합이 되어 통과처럼 보인다. -->
  - [D] summary의 Task 헤더 집합이 plan과 일치 — 블록 전체 누락·Task N 누락·빈 summary·두 경로 오기 차단  (검증: repo 루트에서 `P=.ai/90_issues/active/issue-0049/issue-0049-plan.md; S=.ai/90_issues/active/issue-0049/issue-0049-summary.md; { grep -qE '^### Task ' "$P" && grep -qE '^### Task ' "$S" && diff <(grep -E '^### Task ' "$P") <(grep -E '^### Task ' "$S") || echo '위반: 입력 접근 실패 또는 Task 집합 불일치'; }` 출력 0건. 프로세스 치환 안의 `grep` 실패는 `diff` 종료 상태로 전파되지 않아 두 경로가 모두 잘못되면 빈 입력끼리 비교해 통과하므로, 선행 `grep -q`로 두 파일의 Task 헤더 실재를 먼저 확인하고 실패를 stdout 위반 행으로 환원한다) — 아래 두 게이트는 summary에 실재하는 블록의 행만 검사하므로, 블록 자체가 없으면 검사 대상이 사라져 그대로 통과한다
  - [D] 이 검증 전에 Task 0 및 모든 일반 실행 Task의 summary `결과`가 완료·부분 완료·스킵 중 하나로 확정 — Task N 제외 블록마다 유효 `결과` 행 정확히 1개, 무효·중복 0건  (검증: repo 루트에서 `S=.ai/90_issues/active/issue-0049/issue-0049-summary.md; awk '/^### Task /{if(o&&!n&&v!=1)b++; o=1; v=0; n=($0~/^### Task N/)} o&&/^- \*\*결과\*\*:/{if($0~/^- \*\*결과\*\*: (완료|부분 완료|스킵)[[:space:]]*$/)v++; else if(!n)b++} END{if(o&&!n&&v!=1)b++; print b+0}' $S` 출력 0. 총개수 비교는 한 블록의 미확정을 다른 블록의 중복 행으로 상쇄해도 통과하므로 블록 단위로 센다) — 비워 두면 "완료·부분 완료면 `수행 모델` 값 필수" 조건이 발동하지 않아 아래 강화 조건이 공집합이 된다
  - [D] 완료·부분 완료 Task의 `수행 모델`이 비어 있지 않고 `-`도 아닌 행 정확히 1개(`-`는 미착수·스킵 전용)  (검증: repo 루트에서 `S=.ai/90_issues/active/issue-0049/issue-0049-summary.md; awk '/^### Task /{if(o&&!n&&d&&(t!=1||m!=1))b++; o=1; d=0; t=0; m=0; n=($0~/^### Task N/)} o&&/^- \*\*결과\*\*: (완료|부분 완료)[[:space:]]*$/{d=1} o&&/^- \*\*수행 모델\*\*:/{t++; if($0~/^- \*\*수행 모델\*\*: [^-[:space:]]/)m++} END{if(o&&!n&&d&&(t!=1||m!=1))b++; print b+0}' $S` 출력 0. 리터럴 `-`만 거부하면 빈 값·행 누락·중복이 통과하므로 "없음"의 세 형태와 중복을 전부 실패로 센다) — 전부 `-`로 남기면 아래 교차 벤더 조건이 공집합이 되어 우회가 된다
  - [QD] summary `모델 기록` 표의 `구현 모델`·`audit 모델` 두 행이 "벤더, 모델명" 형식으로 채워지고 서로 다른 벤더  (검증: 교차모델 audit이 두 행 대조 채점)  ← 강등 사유: 벤더 토큰 추출이 자유 문자열 의미 대조라 명령으로 환원하면 표기 변형에 취약하다
  - [QD] 구현에 여러 벤더가 관여했으면 audit 모델의 벤더가 Task 0 및 일반 실행 Task의 비어 있지 않은 모든 `수행 모델` 값에 나열된 벤더 전부와도 상이  (검증: 교차모델 audit이 `수행 모델` 행 전수 추출 후 audit 벤더와 대조 채점)  ← 강등 사유: 위와 같음. 표의 대표값 비교만으로는 audit 벤더의 구현 참여를 놓치고, 한 Task를 여러 모델이 수행했으면 그 값에 나열된 벤더를 모두 비교 대상에 넣는다
  - [ND] audit이 칭찬이 아니라 허점 탐색 방향으로 수행됨  (검증: 사용자가 audit 리포트 내용으로 판단)  ← 강등 사유: 감사 방향성은 리포트 서술의 의미 판단이라 명령으로 환원 불가
